# Resume notes — GMSim

This file captures where we paused so a fresh session can pick up cleanly.

## Current state

- **`main` branch**: at **v0.26.0** (tagged + pushed).
  - v0.24.0 — Doc 14 5-factor trade-value evaluator + trade detail
    panel.
  - v0.25.0 — Alternative-candidates surface. Every trade carries
    up to 5 alternatives considered but didn't fire.
  - v0.26.0 — Closes the v0.20.0 long-horizon roster-shortfall
    residual. Auction cap-room filter now reserves
    `(53 - rosterCount - 1) × LEAGUE_MIN` of room so teams can't
    win so many auctions they pin themselves below the fill-up
    threshold. Pre-fix: 5 teams off-53 across 10 seasons of
    validate-1 (worst 42/53). Post-fix: 1 transient s1 case
    (LV 51/53, recovers by s2).
- Working tree clean.

**407 tests passing** (1 skipped harness). Doc 7 — Roster Management
is substantially shipped, plus trades (Doc 14 5-factor + MVP +
alternatives), transaction log, playoff injury propagation, top-51
cap rule, mid-season FA signings, stats-driven development,
per-player mood (chemistry MVP), the v0.18.0–0.18.2 personality-
driven mood arc, PS-player mood, the Doc 12 news-feed MVP, the
v0.20.0 FA bidding auction, the v0.21.0 proactive NPC dealmaking,
the v0.22.0 inspector FA-sign detail, the v0.23.0 transaction log
filtering, the v0.24.0 5-factor trade value + trade detail panel,
the v0.25.0 alternative-candidates surface, and the v0.26.0
roster-shortfall fix.

## ⏰ Tech debt — deferred

- **Node 20 actions deprecation**: `.github/workflows/deploy.yml`
  uses `actions/checkout@v4`, `actions/setup-node@v4`,
  `actions/configure-pages@v5`, `actions/upload-artifact@v4`, and
  `pnpm/action-setup@v3`, all of which run on Node 20. GitHub
  removes Node 20 from runners on **2026-09-16** and forces Node 24
  by default on **2026-06-02**. Bump action versions before then —
  trivial one-line changes once newer versions ship.

## What's shipped (v0.7.0 → v0.15.0)

| Version | Slice |
|---|---|
| v0.7.0 | Retirement + rookie replacement (age curve), injury propagation through `simulateSeason`, offseason heals in `advanceSeason` |
| v0.7.0 (patch) | Inspector injury surfaces — team-card injured count + roster-table injury column |
| v0.8.0 | Per-player stats persistence — `deriveGamePlayerStats`, `seasonStatsForLeague`, inspector Season Leaders panel + per-player season-stat column |
| v0.9.0 | Career stats history — `Player.careerStats` snapshot at `advanceSeason`, inspector career column |
| v0.10.0 | Year-end awards (MVP/OPOY/DPOY/OROY/DROY/COY) — `seasonAwards()`, inspector Awards panel |
| v0.11.0 | Career awards snapshot — `Player.careerAwards` / `HeadCoach.careerAwards`, inspector "★ 3× MVP" chips |
| v0.12.0 | NFL-realistic stat magnitudes — TDs from points scored, fractional INT rounding, per-unit skill alignment via `unitStrengths` |
| v0.13.0 | Doc 7 Roster Management (slices 1–9) + playoff injuries + mid-season FA + top-51 + stats-driven dev |
| v0.14.0 | **Doc 14 Trades MVP** — `executeTrade` primitive with NFL-faithful cap mechanics |
| v0.15.0 | **Transaction log + inspector surface** — typed append-only history, every primitive logs |
| v0.16.0 | **Trade builder UI** — inspector panel composes + executes trades against the v0.14.0 primitive |
| v0.17.0 | **Chemistry MVP — 7 slices** — `Player.mood` (hidden 0..100), weekly drift from team W/L + HC fit + depth; `moodMultiplier` scales contribution to `teamStrength` + `unitStrengths`; STAR/STARTER mood collapse → `tradeRequestedOnTick` + `trade-request` txn; bidirectional locker-room contagion (frustration drag + veteran-leader lift); `teamChemistry` rolls roster moods into a tier-weighted score + bucket per team; NPC trade-finder matches open trade requests to buyers w/ positional need + cap room and actually executes the deals; inspector chips on TeamCard + TeamDetail |
| v0.18.0 | **Mood rework — personality-driven** — `Player.moodProfile` (archetype/setPoint/volatility/resilience), 5 archetypes (stabilizer / anchor / normal / moody / distraction) with NFL-faithful weights; drift targets per-player setPoint; positive contagion capped at `setPoint+15`; weekly noise + `locker-room-incident` rolls with media-leak hooks (market size, owner quirks, HC `PRESS_CONFERENCE_DISASTER`); `offseasonMoodDrift` between seasons; `migrateLeagueForward` backfills v0.17.0 saves; inspector mood column gets archetype chip + 📰 leak prefix on incidents |
| v0.18.2 | **Mood survivor-drift fix** — depth-chart driver becomes negative-only (no more starter bonus double-counting setPoint); offseason drift 0.7 → 0.9. Survivor mood change over 10 seasons +2.52 → +0.64. Resolves the v0.18.0 known issue |
| v0.19.0 | **PS-player mood + Doc 12 news-feed MVP** — practice-squad players now run a dampened weekly mood pass (drift toward setPoint + scaled team/HC/owner drivers + halved noise; no contagion, no incidents, no trade demands). New `deriveNewsFeed(league, opts)` derivation in `season/news.ts` surfaces leaked locker-room incidents, trade demands, and tier-gated marquee transactions with source attribution (`national_insider` / `beat_writer` / `anonymous_source` / `social_media`). Inspector `NewsFeedPanel` placed above the raw transaction log |
| v0.20.0 | **FA bidding auction (Doc 7 follow-up)** — new `transactions/fa-bidding.ts` with `auctionFreeAgent(league, player)` second-price auction. Every team computes cash valuation (scheme fit × need × cap room, bounded [0.7, 1.2] × tier-standard Y1) and player-preference (archetype × market size + owner/HC quirks, clamped [0.85, 1.15]). Winner = max `cash × preference`; price = `min(runnerUp.cash × 1.02, winner.cash)` (multi-bidder) or `winner.cash × 0.85` (lone bidder). `makeFreeAgentContract` takes a `valuationMultiplier` that scales base + bonus around the tier shape. Runners-up land on `fa-sign` transactions and surface in the news feed. Known limitation: 1–3 teams off-53 on some seeds at 10+ seasons |
| v0.21.0 | **Proactive NPC dealmaking (Doc 7 follow-up)** — new `transactions/proactive-trades.ts` with `runProactiveTrades(prng, league, tick)`. Two parallel candidate generators: (1) buyer-driven positional need — contender (CHAMPIONSHIP/CONTENDER/EMERGING) finds seller with STAR/STARTER at hole position; seller willing if surplus depth OR rebuilder window (REBUILDING/STAGNANT/RETOOLING); STARs only move from rebuilders; (2) mutual scheme-fit swap (Sweat-for-Johnson) — same-position same-tier pair where each is poor fit (<0.85) on own team + good fit (>1.1) on the other. `PROACTIVE_TRADE_CAP_SAFETY = $5M` guard on both sides. Runs weekly in-season after `runWeeklyNpcTrades` and once per offseason between `applyCapCuts` and `refillRosters`. NTC respected. Plus inspector running-version chip + fix for `applyPoach` own-promotion phantom-PS bug (duplicate object-literal key silently dropped the practiceSquadIds filter) |
| v0.22.0 | **Inspector FA-sign detail panel** — clickable fa-sign rows expand inline. New engine surfaces: `computePlayerPreferenceBreakdown(team, player, league)` returns labeled component breakdown (archetypeMarket + ownerQuirks + hcQuirks + hcPlayerRelationships + labels); `FaAuctionResult.bidders` carries full bidder list with cash + preference + perceivedBid + capRoomAtTime + breakdown per team, sorted descending. `TransactionFreeAgentSign` gained optional `bidders?` + `phaseAtSigning?` (back-compat with pre-v0.22 saves; mid-season vet-min omits bidders). Inspector renders contract terms (years × base, bonus, guarantees, proration), bidders table (winner highlighted), and "why TEAM won" callout listing fired preference factors + cash/pref edge vs runner-up. Trade-detail panel deferred to v0.23 alongside Doc 14's 5-factor evaluator |
| v0.23.0 | **Transaction log filtering** — pure UI slice. Existing 10 kind count cards become clickable toggle chips (auto-expands table on click). New filter rows when expanded: 32-team chip grid, 21-position chip grid, min cap hit / dead money $M numeric input (hides non-price kinds when > 0 per user choice). Live "Showing X of Y" counter. Default visible window 100 most-recent matches; "Show next 100" button pages in older matches. Top-right "clear filters" link resets all; per-section "clear" links scope to one dimension. All filters compose; empty Set means no filter. Filter happens up front; render cost stays O(visible rows). Trade detail + Doc 14 5-factor evaluator bumped from v0.23 → v0.24 |
| v0.24.0 | **Doc 14 5-factor trade-value evaluator + trade detail panel** — new `trade/value.ts` with `evaluatePlayerValue(team, player, league)` returning $M total + per-factor breakdown (ability, schemeFit, ageContract, positional, timing) and `evaluateTradePackage(team, incoming, outgoing, league)` returning per-asset breakdowns + netValue. Both pipelines (request-driven `runWeeklyNpcTrades` + proactive `runProactiveTrades`) compute mutual valuations and gate execution on `netValue > 0`. Priority = sum of mutual gain in $M. `TradePayload.metadata` (initiator, source, both teams' evaluations) propagates verbatim to `TransactionTrade` (all optional, back-compat with pre-v0.24 saves). Inspector trade rows now expandable showing both teams' perspectives with labeled per-factor rationale strings + dead-money charge per side. Behavioral shift: 5-factor gate rejects lopsided STARTER-for-BACKUP trades; Pass 1 proactive trades rare until Doc 3 draft picks ship. `alternativeCandidates` field deferred to v0.25 |
| v0.25.0 | **Alternative-candidates surface** — `TransactionTrade.alternativeCandidates?` carries up to 5 trades that were considered but didn't fire. Each entry: buyerId, sellerId, acquireId, returnId, buyerNetValue, sellerNetValue, reason (`'buyer-used'` / `'seller-used'` / `'lower-priority'` / `'failed-gate'`). `runProactiveTrades` tracks every candidate's outcome and walks both processed + upcoming candidates to find those sharing a player with the firing trade. `runWeeklyNpcTrades` `findBuyer` collects all qualified buyers (positional need + cap + positive 5-factor net) and surfaces top 5 runners-up alongside the winner. Inspector `TradeDetail` renders a compact table beneath the per-team panels: buyer, seller, acquires, return, buyer net (green/red), seller net (green/red), human-readable reason ("buyer in other deal" / "lost out" / etc.). Empty → section hidden |
| v0.26.0 | **Long-horizon roster-shortfall fix** — closes the v0.20.0 known limitation. fa-bidding.ts cap-room gate changed from `capRoom >= standardY1` to `capRoom >= standardY1 + (53 - rosterCount - 1) × LEAGUE_MINIMUM_SALARY`. A team can no longer bid on an FA unless they retain enough cap room to fill every remaining roster slot at league minimum. Pre-fix mechanism: favored team wins 4-6 auctions in sequence (each passing the gate individually), ends offseason at $254M cap / $0.4M room — below LEAGUE_MIN so fill-up couldn't reach them. Post-fix: cap-rich teams keep winning but cap-pinched teams drop out earlier, letting fill-up complete the roster. Diagnosed via per-team per-season cap+roster instrumentation, not parameter tweaking. Pre-fix: 5 teams off-53 across 10 seasons of validate-1 (worst 42/53). Post-fix: 1 transient s1 LV case (51/53, recovers by s2) — fresh-league initial cap-cuts state too tight for even the reserve to help, accepted as residual |

## What's next (after v0.26.0)

**v0.27.0 candidates** — known limitation closed; pick fresh direction:
- **Pass 1 proactive-trade scarcity** — with the 5-factor gate,
  positional-need trades are rare because we lack draft picks to
  balance value. Options: (a) wait for Doc 3 draft picks, or
  (b) introduce a "rebuilder fire-sale softener" — rebuilders
  accept slight value losses for STAR/STARTER moves pre-deadline.
- **Node 20 actions bump** in `.github/workflows/deploy.yml` —
  GitHub forces Node 24 default on **2026-06-02**. Trivial one-line
  bumps. Still queued.
- **Roster Manager UI** — depth-chart drag-and-drop, scheme-fit
  indicators. First "playable" UI surface beyond the dev inspector.
- **Doc 3 — Draft Module** — unlocks Pass 1 proactive trades + much
  else. Big slice, own release sequence.
- **Real NFL waiver-wire** — released-during-season players go to
  waivers, not directly to FA pool. Doc 7 thread.
- **Scout reports + scout assignment** — Phase 4 prep; starts moving
  us toward the North Star goal of hiding raw ratings behind
  attributed observations.

**Carried over from v0.20.0 — long-horizon roster shortfall**:
- Validate-progression harness reports 1–3 teams off-53 on 2/3 seeds
  across 10 seasons. Cap band itself is healthy (avg $204–211M, max
  $253–255M). 5-season unit tests pass; the issue surfaces only over
  long horizons. Likely a milder secondary expression of the
  cap-distribution mechanism. Proactive trades use a $5M cap-safety
  guard to avoid making it worse but don't address the underlying
  mechanism. Diagnose after eyes-on feedback on the auction +
  proactive trade behavior.

**Doc 7 — remaining open threads** (low-medium priority, none blocking):
- Chemistry — follow-up slices:
  - Smarter trade-value model (Doc 14's 5-factor evaluation) — current
    matchmaker is a heuristic, not a value-based decision. Both the
    v0.17 trade-request matcher AND the v0.21 proactive pass would
    benefit from it.
- **Wave-based offseason** (bigger FA-market alternative to v0.20.0's
  single-pass auction): superstar tampering window (1–2 ticks) with
  frenzy multipliers boosting valuations, then a first-wave window
  (most non-superstar STARs/STARTERs sign), then bargain-bin (BACKUPs
  / FRINGE / unsigned older players take vet-min deals). Needs
  multi-tick offseason process + intermediate state in
  `advanceSeason`. Estimated 2–3 commits + its own release. Wait for
  eyes-on feedback on the v0.20.0 single-pass auction before
  committing to this direction.
- Real NFL waiver-wire system (released-during-season players go to waivers, not directly to FA pool)
- Scout reports + scout assignment for league-wide player evaluation
- League-wide roster intelligence panels in the inspector

**Doc 12 — remaining open threads** (post-MVP):
- Source reliability + bias discovery mechanics (track-record learning,
  occasional misinformation, retraction patterns)
- Fan sentiment by market — community reaction, organizational pressure
- Multi-source same-event coverage (different outlets cover the same
  trade with different angles/accuracy)
- Sortable transaction database — multi-dimensional filtering across
  the full league history
- Generated quotes attributed to specific reporters / sources
- Draft & prospect coverage — viral hype cycles, mock-draft evolution

**Doc 14 Trades MVP** is shipped (v0.14.0) — `executeTrade(league, payload)`
with NFL-faithful cap mechanics. Future trade work: draft picks (needs the
draft module), third-team brokers, cash considerations, and an inspector
UI to construct + preview trades.

**Other valuable wedges:**
- **Real NFL schedule formula** — attempted in this session but deferred.
  The pairing generator (division round-robin + 3-year intra rotation +
  4-year inter rotation + same-place finishers) is straightforward; the
  hard part is **week assignment** of 272 specific game pairings into 17
  weeks such that each team plays exactly once per week. Greedy / MRV
  heuristics get stuck on the regular structure of the NFL multigraph
  (32 nodes, 17-regular). Picking this up later needs a real edge-coloring
  algorithm (Misra-Gries Vizing, or constructive 1-factorization using
  division-week / non-division-week scheduling blocks). Current `schedule.ts`
  uses random perfect matchings — works robustly, just doesn't match the
  NFL pairing rules.
- **Roster Manager UI** — depth chart drag-and-drop, scheme-fit indicators
  on the active 53.
- **Coaching focus allocation** — Player Development doc's interface,
  currently treated as uniform baseline.
- **Inspector trade builder** — UI to compose `TradePayload` and preview
  cap impact before executing.

## Things deliberately deferred (still)

- **Retiree archive.** Retiring a player drops their `careerStats` and
  `careerAwards`. A future "retired-players archive" slice (probably
  alongside Hall-of-Fame mechanics) preserves this.
- **OL stats** (sacks allowed, pancakes) and **special-teams stats** —
  neither position group produces stat lines, so OL/ST players default
  to the neutral 1.0× development multiplier. Adding stats here would
  let stats-driven dev affect them too.
- **PS contract reductions / extensions / restructures** — current
  contracts are signed-once + expire. No mid-deal restructures.
- **PS player demotion from active back to PS** — once promoted, a
  player stays on the active 53 for the rest of the season.
- **Coaching focus allocation.** Player Development doc's coaching-resource
  interface is skipped; every player gets baseline coaching attention.

## v0.13.0 highlights (this session)

Order in `advanceSeason`:
1. Decrement contracts
2. Retirement (active roster + off-roster pass for PS / unsigned FAs)
3. Activate IR → roster
4. `applyContractExpirations` → drops 0-yr contracts, players become FAs
5. `applyCapCuts` → over-cap teams cut for positive savings
6. `refillRosters` → tier-ordered FA market with multi-year deals
7. `refillPracticeSquad` → 16 fresh rookies per team

Order in `simulateSeason` per week:
1. Recover injuries
2. Play games (track `teamsDuringSeason`, `playersDuringSeason`,
   `contractsDuringSeason`)
3. Per-game injury propagation + IR moves for MAJOR
4. `runWeeklyPoaching` → fill gaps from PS pools (with 4-player
   protection per team)
5. `runWeeklyFreeAgentSignings` → fill remaining gaps from street FAs

Cap rule:
- Offseason phases: top-51 contracts count + dead money
- REGULAR_SEASON / PLAYOFFS: all 53 count + dead money

## Conventions

- Conventional Commits (CONTRIBUTING.md)
- Keep a Changelog format
- Sync versions across all packages: `pnpm version:sync <new>`
- Determinism-first: every random call goes through the seeded PRNG
  (`Prng.fork(label)` for sub-streams). Stats derivation has no PRNG.
- North Star: hidden ratings never displayed numerically to player.
  The inspector exposes them because it's a dev tool — replaced in
  Phase 4 by attributed observations (Scouting Report UI/UX, Doc 18).

## Validation harness

`packages/engine/src/season/validate-progression.test.ts` is `describe.skip`'d.
Flip to `describe(...)` and run `pnpm test -- --run validate-progression` to get
multi-season stats (cap drift, age band, dynasty emergence, skill bounds) when
tuning progression / retirement / awards.
