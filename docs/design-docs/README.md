# Design Docs Index

**Posture (Daniel, 2026-07-11 final): design docs are LOCAL-ONLY working copies,
Google-Drive-backed, NOT published.** Only this `README.md` is tracked in git;
every other `docs/design-docs/*.md` is gitignored (see `.gitignore`, committed
`bc31e8e`/`d34cd52`). The docs still live on disk as working copies you edit
in place — they're just not part of the published repo.

Two tiers of design doc:

1. **Authored working docs** (the ones we write and revise here — Living Voice,
   GM Hire/Fire, the game-sim/talent-spread/Maddeninator design docs, the alpha
   UI/scouting docs, …). These live on disk (gitignored) and are **byte-faithfully
   backed up** to the Drive folder **"GMSim design-docs (repo backup)"**
   (`1rvpwCeAUczv1u8bKMemGPXNLAOxuMpjC`). **RULE: any session that edits one of
   these re-uploads it to that Drive folder in the same session** (gitignored files
   are not in git — a clean clone or `git clean` loses local edits otherwise).
2. **The original numbered corpus** (Phase 1–4 module docs, resolution & research
   docs below). These are Drive-native Google Docs, read **just-in-time by file ID**
   via the Google Drive MCP tool — deliberately not mirrored to the repo, to avoid
   drift between a local copy and the evolving Drive original.

**Fresh-clone note:** a clean clone has NO design docs on disk (they're gitignored).
Pull the authored working docs from the Drive backup folder, and fetch numbered
corpus docs just-in-time by file ID as needed.

If a design doc and the code disagree, the code is wrong, or the doc needs an
explicit revision (re-uploaded to Drive the same session).

---

## Anchor docs (must reference)

| # | Title | Drive ID |
|---|---|---|
| — | North Star (principles consolidated locally — no standalone Drive doc) | see `docs/NORTH_STAR.md` |
| 1 | Living League: 32-Team Ecosystem | `1vDnynFESV5120fZb1EZHUnz4F-VaXzQGy8c_W4Dm-5w` |
| 0 | Master Reference (this index's source) | `1J9fxCVxItX1c2Pw97yaK0h-0zoA0uUN1oen9tH7yX20` |

## Tier 1 — Authored working docs (local on disk, gitignored, Drive-backed)

Backed up to Drive folder `1rvpwCeAUczv1u8bKMemGPXNLAOxuMpjC`. Edit locally →
re-upload same session.

| Title | Path | Notes |
|---|---|---|
| Living Voice — Scouting & Media as a Living, Non-Deterministic Layer | `docs/design-docs/LIVING_VOICE.md` | Authored 2026-06-05 (Daniel-directed). Read-to-learn blurb encoding, two-seed determinism split (world seed = players, voice seed = voice), Voice Pack corpus seam. Candidate to promote to Drive as authoritative. |
| GM Hire/Fire & The Front-Office Lifecycle | `docs/design-docs/GM_HIRE_FIRE.md` | Authored 2026-06-11 (Daniel-directed, design before code). Real-NFL firing baselines (GM W/L sequences vs coach-firing timing), Black Monday evaluation, firing ladder (HC before GM, "his guy" coupling), hiring market, Headhunter calibration agent. Resolves Doc #8's in-season-firings open thread; implements Doc #9's rebuild triggers. |
| Talent Spread v2 — Turnover Identity & the Fumble Dimension | `docs/design-docs/TALENT_SPREAD.md` | v1 (efficiency-channel scaling, Fable 2026-07-13) FALSIFIED at implementation — §0 records the three lessons (yardage channels can't close the delta; garbage-time ~flat; sim luck is yardage-shaped → COMPOSITION problem). **v2 (Fable 2026-07-14): fumble-dimension turnover identity** — offense `ballSecurity` + defense `fumbleForcing` facets feeding the (currently flat) fumble rate, mean-neutral, INT channel FROZEN (already ×1.02 real); pre-registered fatal test (pass delta must NOT rise + P(winner out-passed) toward real bar) with a D3 response-curve checkpoint. Drive backup `1OWv5TJDoZBgVVOOsEAkYcWr_uEHrJH5-` (supersedes all prior — trash `1GDfCde2…`,`1bJPWckC…`,`1uAZZR7…`,`1Gwjlro6…`,`12e84IL4…`). **⛔⛔ v2 ALSO FALSIFIED (Opus 2026-07-14, §0b):** ballSecurity/fumbleForcing are roster-quality facets, so the good team also wins the turnover battle — P(winner out-passed) moved WRONG way (38.8→37.6%). UNIFYING LESSON: no quality-correlated channel decouples winning from yardage; the decoupler must be quality-ORTHOGONAL (high-variance turnovers / garbage-time). Reverted, no code shipped. D0 probes live + reusable. **v3 rethink DELIVERED 2026-07-15 → `SEASON_FORM.md` (supersedes this doc as the campaign design of record; this doc remains the falsification archive).** |
| The Maddeninator — Madden-Ratings Realism Agent | `docs/design-docs/MADDENINATOR.md` | Planned 2026-07-14 (Fable, Daniel-directed). New truth-arbiter agent (Adjudicator sibling): ingests Madden ratings by year+team from nfldraftbuzz.com/madden (site recon done — URL scheme/columns/player-IDs verified; browser-UA required), Ollama-assisted attribute-map + player-ID joins, four bar families (roster shape · aging/dev trends · YoY shocks · team ratings↔W-L). THE external anchor for the stalled talent-spread question (spread + shocks). **M0+M1 SHIPPED `c5db688`** (30,783 player-seasons, Madden 2012-2026 = NFL 2011-2025, curl-backed scraper + Ollama attribute-map draft). **⚡HEADLINE: Madden team-OVR↔wins corr ≈0.365 vs sim 0.435 — the sim is NOT under-coupled; the wins-sd deficit is LUCK not talent spread → refutes the talent-spread premise, confirms the v2-falsification direction.** Drive `1vIDrPI1C879oR9adjRxvRJkhyIyCw6--` (supersedes `1CvVCCA0…`,`16YsKVWw…` — trash). M2 next (real-side bars + checkpoint 2). |
| Maddeninator Findings — Real-Side Anchors | `docs/design-docs/MADDENINATOR_FINDINGS.md` | Opus 2026-07-14, **§7 added 2026-07-15**. The decision-grade consolidation of the Madden real-side anchors (M2 read) — the input for the W-L/talent-spread campaign re-plan. Team-OVR↔wins 0.365 vs sim 0.435 (deficit is LUCK not spread) · QB partial-corr 0.284 dominant, EDGE weak (0.074), non-QB flat ~0.10 · offense drives wins 2.2× defense. **§7 YoY shock anchor (MEASURED): real player OVR φ≈0.876, shocks 0.465 z. §7b (Fable 2026-07-15, `_sim_yoy_phi.mjs`): sim side measured apples-to-apples — sim player φ 0.966, shocks 0.156 z (~3× too SMALL); sim team 0.71 vs real team 0.525 → the sim UNDER-churns at both levels (the earlier "sim over-shocks" read was a team-vs-player artifact). φ fenced OUT of the W-L campaign (wrong timescale); under-churn → M3/aging.** Re-plan seed: quality-orthogonal variance → executed as `SEASON_FORM.md`. Drive `1ymlCvs0ljBH8xklXrz8BeAOjEGF3t59d` (supersedes `1Slt86xaczmFisTawmDWvtKp16EqeXlpq`, `1IANOqTpSyYU9bT6aA_uA8GXB0oQi5yTw` — trash). |
| Season Form — quality-orthogonal variance re-plan (talent-spread v3) | `docs/design-docs/SEASON_FORM.md` | **APPROVED Daniel 2026-07-15. D1 fumble form SHIPPED v0.185.0 `ca00ed3`. D2 red-zone form FALSIFIED as inert (Opus 2026-07-17, §12) — reverted.** The W-L campaign endgame re-plan. Spine: the binomial-ceiling proof — ~90% of the missing wins-variance MUST be season-persistent. Mechanism: hidden team-season **form latent ε** (quality-orthogonal) expressed through non-yardage channels — D1 fumble + D2 red-zone. **§12 UNIFYING FINDING: BOTH channels are bar-saturated in the sim** (fumble margin sd 3.91 vs real 4.17; RZ TD% sd 5.91pp vs real 6.7pp — 0.79pp headroom) → neither can supply the missing variance (D2 fatal test: P(out-passed) 39.0→39.0 flat, wins-sd 2.62→2.65). Cross-ref Maddeninator ("wins deficit is LUCK not talent"; strength→wins 0.365 vs sim 0.435). **PIVOT: the missing season-persistent luck lives in the deferred channel — D4 injury availability (QB above all); handed to Fable as its own doc `INJURY_REALISM.md` (brief 2026-07-17). Sequencing: Roster Floor → D4 injuries (no D3 curve — neither channel transmitted).** Drive `1DoopqyRnR1rDyfBUOzftyXx-68BiDa4e` (full §0–§12, uploaded 2026-07-17; supersedes `10Tir_WEScSkxYrCDIlua45Nph8bTbXnI` + §11 companion `1jXlHjKyMEo71SgDXvzO9Xs8Rau4X13Vc` + `1c9LKK93sg51SoHZPiCTZBZCV71ELaS2k` — trash all three). |
| Real-Roster Baseline + Injury Continuation | `docs/design-docs/REAL_ROSTER_BASELINE.md` | **DIRECTED — Daniel 2026-07-17/18 (Fable authors, 2026-07-18).** The post-D2-falsification pivot, two parts. **Part B FIRST + gates all:** build real-NFL league seeds — **SAME EXACT PLAYERS per team** (frozen season-start snapshot), Madden ratings → engine skills via the signed-off attr map + a load-bearing scale-calibration transform; prove on 2024, then expand 2016-2024 (~288 real team-seasons); engine must run within pre-registered bounds (corr(sim wins, real wins) 0.35-0.50 honest ceiling — Madden OVR↔wins is 0.365; ≥90% of teams inside their sim 5-95 band) before generated seeds are trusted again — becomes the permanent regression baseline for every engine change. Stages: B0 builder+calibration (Daniel eyeball) → B1 20-seed 2024 replicates (checkpoint) → B2 all years → B3 **M-INJ** (Maddeninator × injury cross-ref: value-weighted availability loss, transmission wins², QB1-out yardage expression = the empirical P(out-passed)-gate answer) → B4 amended injury brief. **Part A recorded:** injury model = TWO channels — availability + play-through DEGRADATION (attribute-specific penalties, GM decision surface); **CONCUSSIONS = standalone module (Daniel-marked)**. **10 HARD FLAGS open (§B.4/B.4b)**; standing workstream rule: flag even slightly-off things loudly. **B0-B4 ALL EXECUTED 2026-07-18** (Daniel blessed "#2"; builder `packages/truth-arbiter/data/_rr_seed.mjs`, harness `_rr_b2.mjs`, per-year seeds cached). **§B.8: B2 PASSES → baseline ADOPTED as the permanent regression harness** — 288 real team-seasons, corr(sim,real) 0.425 IN BOUND (sim beats its own prior 8/9 years), **wins-sd 2.72-2.87 EVERY year vs real 3.3 = the variance gap is universal → third independent confirmation of the missing luck channel.** **M-INJ landed all 4 measurements** (`_minj_report.md`): QB1 −0.250 wins/start missed; availability = +1.85 of the ~3.9 missing wins² (47%, undercount); QB1 out in ~22% of real team-games vs engine ~0.3%/game (smoking gun); **fatal-test ruling settled: P(out-passed) NOT a D4 gate (39.9% in QB1-out-loser games < 46.5% all) → replaced by corr(strength,wins)→0.365.** **Injury brief v2 = `INJURY_BRIEF_FOR_FABLE.md`** (Drive `1oZoxckcbare96wIVyhjcaVii0x5m3Ntl`) — Daniel runs it to design `INJURY_REALISM.md`. Drive `1UqLfMFLzzajRfVQSTqLeULGXyknkkB7D` (supersedes `1kiAgpDQfzjItgKUAlEa5WOjdi1aqpi3c`, `1y3Qi0fpSmgJYHCr8evBArqsYxd3QZCuG`, `1YfgkFrSTUz19mHWlvnDfN-I-wwqA3BRI` — trash all). |
| Injury Realism — the load-bearing variance channel (Season Form Part 2/D4) | `docs/design-docs/INJURY_REALISM.md` | **APPROVED — Daniel 2026-07-18 (all six §13 calls). Opus implements Stage I starting at D0 (§7); no engine code before the D0 report.** Designed by a Fable agent from `INJURY_BRIEF_FOR_FABLE.md` v2 (Drive `1oZoxckcbare96wIVyhjcaVii0x5m3Ntl`). **Headline audit finding (code-verified): the TRANSMISSION HOLE — nothing in `games/` reads `Player.injury`; `strength.ts` uses full `rosterIds` unfiltered → 85% of injury events (MINOR/MODERATE) are on-field no-ops.** Stage I = rate fix (QB 0.003/game vs real QB1-out ~22% of team-games, benching-discounted to ~13-16% at D0-P1) + `availableRoster()` transmission fix + extracted `season/injuries.ts`, jointly. Stage II = attr-specific play-through degradation (INJURY_CATALOG, effective-skills view) + GM play-or-sit surface (npc-ai path). **§13.4 modified ruling: interim generic `'head'` injury = fixed 3-week absence, no GM override, ships Stage I; full Concussion Protocol module (real data + league protocol, no-override state machine) = named follow-on Part 3.** Fatal test: wins-sd ↑ AND delta not ↑ AND corr→0.365 (P(out-passed) demoted to instrument per M-INJ.3). Gates incl. the B2 real-roster harness (frozen + injury layer; DAL-2024 marquee). Roster Floor lands before Stage I validation. Drive `1fQCeFWkRYd20MhPOC8iyaCDECHkd5gLr` (supersedes `1NlNIOxak-sNLiQMnS3VaiP6Q07TAEdqj` — trash). |
| Roster Floor — mandatory-53, restructure-first | `docs/design-docs/ROSTER_FLOOR.md` | **⚠️ §13 (Sonnet, 2026-08-05): Talent Allocation's T2a slice (`TALENT_ALLOCATION.md` §12) surfaced TWO narrow margins in `enforceRosterFloor` — A/B-confirmed T2a-caused ladder-iteration-bound crash (team TEN, cap spiral via `OFFSEASON_TRANSACTIONS`) + a suspected FLR contract-ID collision. 14-seed census: 7.1% hard-crash rate. BLOCKS T2a shipping.** **▶ §14 (Opus, 2026-08-05) RESOLVES the design read — Bug B PROVEN + root-caused exactly, and BOTH bugs traced to ONE upstream disease.** Probe `_rf_s14_collision.mjs` (transaction log as ID ledger): seed `qcensus-1` team PHI season 4 = **129 floor ops onto 75 distinct ids, 19 ids issued to two different players**; chain = 3 engagements at ticks 156/170/171 → `counter` resets per call (`roster-floor.ts:189`) → aliased ids → `applyFloorCut`/`releasePlayer` **delete by contract id** → orphan → `contract missing` throw. **Root cause bounded by a ten-site audit: `enforceRosterFloor` is the ONLY contract-minting site that runs >1×/season while keying its id on `seasonNumber` — §3's mid-season wiring silently invalidated an unstated once-per-season assumption that its own weekly siblings (`midseason-fa`, `poach`) already honour by keying on tick.** **P1 FALSIFIED and recorded** (silent corruption does NOT dominate — multiplier 1.0×); P2/P3/P4 confirmed exactly. **§14.3 the reframe: floor-op volume is bimodal (`0,129,0,3,0,4` — 3/6 leagues never engage the ladder at all), so Bug A and Bug B are two failure surfaces of ONE disease — a team in a cap death spiral hammering the ladder; per law 3 both fixes are symptom containment and the disease is upstream (§14.5 Fix 4 = its own diagnostic slice).** **§14.4 severity escalation: `createLivingLeague` IS `createLeague` + 5 sim years, so the corruption window sits INSIDE new-game genesis — this can corrupt a league during "start a new game" and detonate on the player's first season; LEAGUE_GENESIS D0 missed it because that gate counted the soft `roster-floor-violation` log, which this failure never produces (it throws).** Design: Fix 1 tick-key the FLR ids (2 string literals, no migration) · **Fix 2 mint-time uniqueness guard — RULED BY DANIEL 2026-08-05 (§14.9): CLASS-WIDE (all ten mint sites, *"i dont trust it not to sneak back in"*) + LOG-AND-CONTINUE (deterministically uniquify, record a new `contract-id-collision` transaction, keep simming — NOT a hard throw). The two rulings reinforce: log-and-continue removes the discovery hazard that made class-wide risky.** Four load-bearing constraints in §14.5 (uniquify AT id-generation before the Contract is built, or map key and `player.contractId` diverge · deterministic only, no PRNG · log via the `roster-floor-violation` precedent · union variant likely needs no migration, verify) · Fix 3 **do NOT raise the iteration bound** · Fix 4 upstream cap-spiral slice (still unruled: own doc vs `TALENT_EROSION.md` slice 1). **⚠️ Determinism risk CHECKED AND CLEARED (Opus): no contract-id sorting exists anywhere in the engine; all four `league.contracts` walks are order-insensitive; non-colliding leagues bit-identical (5 of 6 seeds) — no separate determinism gate needed.** Pre-registered predictions + falsifiers §14.6; gates §14.7 (new permanent gate = zero `contract-id-collision` entries); handoff §14.8. **Fix 1 alone does NOT unblock T2a — Bug A is untouched by it.** **Also ruled §14.9: NO ground-up engine rewrite** (evidence: 193 feat vs 35 fix commits, fix log dominated by recalibration a rewrite would reset not remove, archaeology battles are emergent-equilibrium and would recur, `0,129,0,3,0,4` shows half of leagues never engage the distressed machinery; accepted prescription = targeted hardening, of which Fix 2 is instance one; standing note: only 3 refactor commits in 398). **APPROVED + SHIPPED v0.186.0 (`1826147`), PATCHED v0.187.2 (direct, no-agents session, 2026-07-21).** §12 termination proof falsified under Injury Stage I stress (seed `goat-18` team NYG: same-contract restructure nibbling + a cut-spiral, deficit 2→11, dead money detonating to $232M/$341M cap by season 5) — fixed with a per-engagement restructure-exclusion set + a cut budget (`Math.max(5, deficitStart*3)`); reproduced the exact 11-hour hang scenario post-fix, now resolves in <2 min. 2 new regression tests, 16/16 passing. **Root cause of the underlying cap spiral routed to `LEAGUE_GENESIS.md`** (leagues start with no contract history, so all complexity is manufactured forward under stress) — this fix keeps the ladder bounded/honest, that design fixes why teams get into this shape at all. INV-FLOOR: exact-53 AND cap-compliant post-`advanceSeason`. Mechanism: `enforceRosterFloor` 4-rung ladder — (1) restructure-first (v0.176 `restructureContract` + new `maxConvert` partial-conversion param; NO competitive-window gate on this path; doesn't count vs MAX_RESTRUCTURES_PER_SEASON), (2) fringe cut (`pickMinimalCasualty`, roster-aware target), (3) loop w/ termination proof, (4) loud `roster-floor-violation` — never silent sub-53, never a cap tolerance. **Mid-season RULED: same ladder wired as the weekly midseason-FA affordability fallback (IR-driven sub-53 triggers restructure-first same week); Week-1 boundary = the exact-53 GATE; in-season = never-below-53 assertion.** Acceptance: the 4 `<=53` INTENDED-TEMPORARY sites (retirement/advance/offseason/proactive-trades tests) re-tightened to exact-53 + **pin-rate flips instrument→permanent GATE=0**. D0 P1-P4 listed for Opus (ATL `retire-trajectory` repro, ladder sufficiency, affordability-skip census, post-fix A/B). Battery waiver (skip Magistrate/Goatinator) APPROVED §12.4. Drive **split for size** (local file is the complete source): `1evlTMm7-kwyBWo1JCTJs1BCBz0Mocftw` (§0-12, §12 sign-off record) + `1dTz8adq6avA3pOIszzcUvfjYK_3Qfnr1` (§13, 7,981B byte-verified) + `1hAJXyb7VwjhroV1HMFkMCCmbB70YbDhc` (**§14 RULED, 17,325B byte-verified**) + `1CYGtrAvkMiZ6yViTzQ3A3rN6QdYRIfKu` (§14.7a REQUIRED regression tests + revised §14.8 steps 2-5, addendum to the RULED file, 4,434B byte-verified) + `1q9r-euXeC8DF1inbiNeAH3tIbrgMzxPS` (**§14.10 EXECUTED — Sonnet 2026-08-05: Fix 1 + Fix 2 both landed, committed as `c870321` + `4237e7f` on top of v0.190.0, NOT pushed/tagged. Gates all green: 0/10 fresh probe run, the exact historical `qcensus-1` crash seed now clean (281 floor ops → 167 ids, 0 collisions), T2/T3/T4 5/5, 127/128 neighbor batch (the 1 failure A/B-isolated to T2a, not this work — recorded as a new T2a breadcrumb). T2a REMAINS BLOCKED — Bug A (cap-spiral iteration-bound crash) is untouched by design.** 2,104B byte-verified) + `1s3Hw8mZU5JHJ4KWxIlnwLBECjMGQOrHH` (**§15 — BUG A ROOT-CAUSED, Opus 2026-08-06, 11,861B byte-verified**) — supersedes `1-zSsAZS6y5bd1_nveUfilspxYFXgafYH` and the pre-ruling §14 `1ApR9F2j-nOKLHzEhyRI1N7thweGzWuoy` — trash both. **▶ §15 (Opus, 2026-08-06) SUPERSEDES §14.5's Bug-A framing AND §13's "T2a-caused" attribution — THE CRASH IS A MIS-DERIVED ITERATION BOUND, NOT THE DISEASE.** `roster-floor.ts:248` sets `iterationLimit = 4 * startRoster + 20`, but the work scales with the DEFICIT (`53 − roster`) — **inversely related**: a team 1 short gets 228 iterations for 1 signing; a team 43 short gets **60 for 43 signings plus every interleaved restructure/cut**. The termination proof in the comments (restructure exclusion + cut budget ⇒ ~2× roster size) **never counts the SIGN ops**, which dominate at large deficits. Corrected derivation: `totalIters ≤ startRoster + maxCuts + (53−startRoster) + maxCuts = ROSTER_FLOOR_SIZE + 2·maxCuts` (CHI: 53+258 = **311 ceiling**). **Measured (patched scratch `dist`, repo untouched): the identical CHI engagement runs `iters=259 origLimit=60 cutsUsed=129 endRoster=10` — 259 ≤ 311, bound wrong by ~4.3×.** With the bound corrected, **all 12 seasons of `goat-15` complete**, the distressed team **still fails to reach 53 and still logs the loud violation** (17 `roster-floor-violation` entries, all CHI, mid-season ticks 109-121), and **CHI self-repairs at the season boundary** (dead $243.3M→$46.4M; seasons 4-12 all roster-53 with positive room). **NOT symptom-tuning (§15.4 answers the throw message's own "do not raise the cap" head-on): the bound is an assertion of a DERIVED PROOF, not a trigger; the corrected value falls out of the same structural argument, the derivation predicted the measurement, and the symptom is fully PRESERVED — the team stays broken and loud, only the spurious abort goes.** **Decomposition: Bug A LEAVES Fix 4** — the crash fires on any large-deficit engagement whatever caused it (T2a's roster shape → TEN; the cap spiral → CHI; same guard rail, same omitted term, two unrelated upstream causes). New **Fix 5** = correct the bound (1 constant + derivation comment + amended throw message + a "hopeless team degrades gracefully instead of aborting" regression test; do NOT touch `maxCuts`/restructure exclusion/body-supply guard/rung order). **Fix 5 is a PREREQUISITE INSTRUMENT for scoping Fix 4** — today the disease aborts the run so it can't be measured at scale; with the bound fixed, `roster-floor-violation` counts become a clean non-fatal league-wide severity measurement. **Fix 4 keeps its scope, loses its emergency status.** §15.7 pre-registers P1-P4; **P4 = "Fix 5 also clears §13's T2a TEN crash" — MUST BE TESTED not assumed** (if it holds T2a's hardest blocker dissolves; if it fails §13's A/B attribution was right and T2a stays blocked on its own merits). §15.9 names a new open question: CHI carried **84.9% of the cap in dead money** and then repaired in ONE offseason. **▶ §15.10 (addendum, Drive `1DIrX-Q1X_vRL-9fG5_yKDhc5Qr0ZOYyL`, 7,886B byte-verified — SUPERSEDES the §15 companion's final §15.9 bullet): FIX 5 APPROVED — Daniel 2026-08-06 ("As for design approval, granted"), Sonnet executes.** §15.10b gives the exact 3-part change (bound → `ROSTER_FLOOR_SIZE + 2*maxCuts + 10`, `maxCuts` already declared 2 lines above so NO reordering; corrected 3-term termination comment; delete the now-misleading "do not raise the cap" from the throw). §15.10c gives the 7-step order (**RED test first**, apply, GREEN + P1 repro, targeted+benchmark gates, P2 census + Goatinator `sim 12 32` with caches cleared, **P4 T2a unstash→TEN repro→immediate re-stash**, commit — no push/tag/full-suite). **⚠️ §15.10a — HONEST CAVEAT ON THE APPROVED FIX: correcting the bound lets the ladder spend its FULL cut budget** (measured `cutsUsed=129` = `3×43`, `endRoster=10` — ~129 cuts + ~129 signings for ZERO net roster progress, each cut booking dead money). Not a reason to withhold (the alternative is the sim aborting — the wrong bound is never protective), but **Fix 5 carries a MEASUREMENT OBLIGATION**: report churn volume + dead money booked. If churn proves destructive the follow-up is a **no-progress guard** (bail to rung 4 when roster hasn't grown in N iterations — a second extension of §2.3's monotonic-progress assumption after the body-supply guard) — **explicitly NOT in Fix 5, measure first, don't implement speculatively.** **§15.9's dead-money question now carries a LEADING HYPOTHESIS (Daniel-endorsed: "that doesn't make any sense"): the one-offseason RECOVERY is probably legitimate** (cutting accelerates unamortized bonus into the current year — real NFL "eat a year" behaviour, consistent with 36 `cap-cut`s), **the ACCUMULATION is the disease = a RESTRUCTURE CASCADE** (floor restructure converts base→bonus→prorates; repeated restructures pile up unamortized bonus; cuts then accelerate all of it → positive feedback). Fix 4's first check: trace CHI's `forFloor: true` restructures across seasons 1-2 and test whether season-3 dead money traces to them — if so Fix 4 targets the cascade, not the cut logic. **▶▶ §15.11 EXECUTED — Sonnet 2026-08-06, commit `abad8b0`, Drive `1qTmBqxafgwnHO_4rkQlHrp-5Otb52iaO` (6,623B byte-verified). ALL FOUR PREDICTIONS CONFIRMED.** P1: seed `goat-15` completes all 12 seasons. P2: 20-seed vanilla census 0/20 crashed (unchanged from pre-fix, as expected) AND a full Goatinator `sim 12 32` completes all 32/32 seeds incl. `goat-15` itself. P3: `roster-floor.test.ts` 18/18, `mint.test.ts` 5/5, `advance.test.ts` 11/11, `league-tick-benchmark.test.ts` 1/1 (16.0s/240s budget) — Week-1/pin-rate invariants untouched. **P4: FIX 5 ALSO CLEARS T2a'S INDEPENDENT §13 TEN CRASH** (unstashed, rebuilt, `qbverify-4` 12 seasons clean, TEN holds 53 throughout, re-stashed immediately after, never staged/committed) — **T2a's hardest blocker is gone; it returns to being gated only on its four recorded non-crash ripples.** **§15.10a churn measurement came back BETTER than the caveat anticipated: ZERO violations/cuts/dead-money across the 20-seed census** (only 33 clean forFloor signs + 19 restructures, rung 2 never even reached) — the CHI-style pathological oscillation (129 cuts/130 signs/$0 progress) did NOT recur across fresh seeds; the no-progress-guard follow-up is **not warranted by measurement, stays unbuilt.** Two bonus findings: (1) caught+fixed an unrelated pre-existing test-helper bug (`floorTxns` silently reads 0 for `roster-floor-violation` since that kind never carries `forFloor` — masked the regression test's first correct attempt as a false failure; an existing test at old line 262 has the same latent gap, NOT touched, flagged as a follow-up); (2) a `git stash` (no pathspec) near-miss swept Fix 5's files into T2a's stash entry during the P4 re-stash step — caught before any commit via `git status`/`stash show --stat`, recovered cleanly with `git checkout stash@{N} -- <path>` + `git stash push -- <path>`, T2a never staged/committed; documented as a process lesson (`git stash push -- <path>`, never a bare `git stash`, whenever other uncommitted work coexists with a stash). **▶▶ §16 Fix 4 DIAGNOSTIC + SAFEGUARD SHIPPED — Sonnet, 2026-08-27/28 (Daniel's calls: "do #1", then "yup, lets add in that safeguard").** Fresh repro from `TALENT_ALLOCATION.md` §13's hard flag (0/20→3/20 violation rate). **§15.9's restructure-cascade hypothesis DISCONFIRMED as the primary driver**: only 2/1080 cap-cuts on NYJ/`adv-trajectory` traced to a prior restructure. **Real mechanism, code-verified: a same-player sign/cut THRASH LOOP** — one player signed-then-cut 169x in direct succession at one tick, net zero progress each cycle, because `signFloorMinimum` always mints the cheapest possible contract (structurally `pickMinimalCasualty`'s top pick the instant he's signed) while `sortedFreeAgentPool` can hand him right back as best-available in a depleted pool — distinct bug class from the v0.186.1 body-supply guard (aggregate pool size only, not same-player selection symmetry). **FIX SHIPPED**: a `signedThisEngagement` exclusion set mirroring the existing `restructuredThisEngagement` pattern (v0.187.2) — new regression test confirmed RED pre-fix (28 signs/27 cuts on one player, via temporary stash) and GREEN post-fix (1 sign, 0 cuts); a pre-existing test was silently asserting the SAME oscillation as "expected" (Fix 5 explicitly deferred it) — updated to the corrected outcome. **Measured**: NYJ's total cap-cuts fell 1080→537 (roughly halved); the matched 20-seed census is **still 3/20 post-fix** (same 3 seeds) — this fix removes wasted churn, it does NOT reduce how often a team fails to reach 53 (the genuine cap distress is untouched by design; "Fix 4 proper" remains unscoped). **New residual found, not fixed**: the same player can still cycle across SEPARATE weekly engagements (confirmed: 17 cycles across ticks 208-225) — `signedThisEngagement` is correctly scoped to one call; fixing cross-call cycling needs its own design pass. Full suite green (428/438, 10 skipped) + typecheck clean. Drive `14Omb-TgjXjYzLGkoB9SqA_2RSVRZ-l0t` (§16, 9,570B byte-verified; supersedes `1WhoTAmR9_xbKa9zBZsuYBAEGWt6az6-n` — trashed). |
| Opus Triage 2026-08-05 — 3 pre-existing full-suite failures + queued Roster Floor work | `docs/design-docs/OPUS_TRIAGE_2026-08-05.md` | **TRIAGE REQUEST, not a decided design. Sonnet, 2026-08-05.** Part 1: full suite (Daniel-called, post §14 Fix1/Fix2) came back **3 failed \| 131 passed \| 5 skipped (139 files); 1174 passed \| 12 skipped (1189 tests)**. All three **A/B-confirmed pre-existing** (`git checkout ebd26ec -- packages/engine/src`, byte-identical failures, restored after) — NOT caused by Fix 1/Fix 2: (1.1) `npc-ai/front-office.test.ts` coordinator→HC promotion loses its OC/DC `careerStints` entry (seed `carousel-3yr`) — known seed-sensitive test family, `gmsim-failure-archaeology`'s 2026-07-04 carousel entry is directly relevant, discriminate reshuffle-vs-regression first; (1.2) `season/stats-coherence.test.ts` — game `G_S1_W14_MIN_ARI`, ARI has 33 receiver targets but 0 passer attempts (a dropped passer line, not found where yet — `games/stats.ts` `deriveGamePlayerStats`); (1.3) `transactions/fa-bidding-watch-list.test.ts` — deterministic 8-seed watch-list-boost rate **0.0583, below its own v0.187.1 calibration band (0.08-0.09), not just below the 0.06 gate** — hypothesis (unverified): v0.190.0's `releaseSurplusStarters` changed which players this test's auction pool contains. Part 2: Roster Floor §14's still-open items, none executed — **Fix 4** (upstream cap-spiral disease, own doc vs `TALENT_EROSION.md` slice 1, undecided) · **Bug A** (`qbverify-4` team TEN iteration-bound crash, deliberately unfixed, blocks T2a until Fix 4 closes it or Daniel accepts the residual risk) · **T2a** (still uncommitted, blocked) · a **new T2a breadcrumb** (`transactions/extensions.test.ts`'s cash-lag-floor test flips outcome with T2a present, A/B-confirmed, root cause unverified) · the push/tag call (blocked on Part 1 regardless of Roster Floor's own state). Drive `1lmDvZ7hUn8b0yki5PxUi4n4AnCwtctNf` (11,887B byte-verified). |
| Opus Findings 2026-08-06 — Roster Viability closeout + full fleet run + a NEW crash on vanilla main | `docs/design-docs/OPUS_FINDINGS_2026-08-06.md` | **REPORT, not a decided design. Sonnet, 2026-08-06 (overnight continuation, Daniel asleep, self-paced).** Roster Viability Slice 1 closeout: P1/P4 confirmed (void rate 0.2096%, in-band), **P2 FALSIFIED** (void team-seasons floor-op mean 0.00 vs population mean 5.40, n=70/n=5 — the "one disease" unification is wrong; Slice 2's cap-spiral half now needs independent treatment). **HEADLINE: running the full truth-arbiter fleet, Goatinator CRASHED** — `enforceRosterFloor`'s 60-iteration bound, team CHI, tick 109, seed `goat-15` — on **vanilla `main` with T2a NOT applied** (T2a's `roster-blueprint.ts` confirmed stashed). This falsifies `ROSTER_FLOOR.md` §13's "T2a-caused" framing by direct counter-example: the crash is a demonstrated property of shipped main, not a T2a-exclusive risk. Minimal 5-second repro confirmed byte-identical (`createLeague({seed:'goat-15'})` + 12× `advanceSeason(simulateSeason())`, throws season 3). Rate measured: pooled 1/29 ≈ 3.4% across the Goatinator run + a follow-up 20-seed census (`_bugA_main_census.mjs`, 0/20) — rare but real, consistent with §14.3's bimodal floor-op shape. `gates full`: 9/9 clean of errors, 5/9 flagged (class-talent, adjudicate, liquidator, actuary — treated as pre-existing background, not deep-dived; headhunter's 2nd-own-hire GM-survival metric 23.1% vs target ≤10%, n=26 vs the v0.138 baseline's n=10 — checked and NOT caused by tonight's coordinator-stint fix, which never touches GM/HC code paths; flagged not asserted). magistrate/ombudsman/scorekeeper/star all clean (0 flags) — directly confirms tonight's drive-sim-touching changes are clean. Goatinator + Barterer run separately (excluded from `gates` by design). Does not diagnose Bug A's mechanism — that's Fix 4 territory per law 3, this report only escalates priority with a repro. Drive `10db1QHcuIyfvbdyq_ZMi8kghfHJXr6gq` (12,431B byte-verified). |
| Roster Viability — a team must always be able to field a real football team | `docs/design-docs/ROSTER_VIABILITY.md` | **DESIGN OF RECORD. Opus 2026-08-05; Daniel APPROVED both Slice-1 engine fixes ("i give you a yes on both"). Sonnet executes** (per the new standing split: Opus plans, Sonnet executes). **THESIS: `INV-FLOOR` is a COUNT invariant, not a FIELDABILITY one — a team can sit at exactly 53, fully cap-compliant, with ZERO healthy QBs and every existing invariant is satisfied.** That gap unifies two families previously treated as separate: **positional voids** (triage 1.2 — ARI played week 14 with no available QB, 1/570 team-games, `att=0 tgt=33 passYds=0 recvYds=0`, a signature producible by exactly one branch: `drive-sim.ts:929-933` guards the passer's ATTEMPT on `if (passer)` but not the receiver's TARGET) and the **cap spiral** (`ROSTER_FLOOR.md` §13.1/§14.3 — TEN shed 40 players while cap commitment ROSE $129M; floor-ops bimodal `0,129,0,3,0,4`). **Supersedes §14.5's Fix-4 scoping** (was "cap spiral", now "viability", of which the spiral is one cause). Two defects: **D-A** `signFloorMinimum` fills to 53 position-BLIND (head of `sortedFreeAgentPool`) so nothing guarantees ≥1 QB · **D-B** `buildTeamPersonnel` returns `qb:null` and the sim silently plays an impossible game (the offense literally cannot complete a pass — completions/yards/receptions all nest inside `if (… && passer)`). **The D-B fix COMPLETES AN EXISTING PATTERN, not a new mechanism: `kickerRating: kickerP ? … : KICKER_NEUTRAL` + `PUNTER_NEUTRAL`/`RETURNER_NEUTRAL` already exist (`drive-sim.ts:798-800`) — QB is the ONLY skill position with no fallback.** Slice 1 = §4.0 **resolve the qbPlay unknown FIRST** (`matchupFacets` also filters through `availableRoster`; whether `facet()` degrades on an empty group decides if the emergency passer is a pure stat fix or needs an effectiveness penalty — REPORT, don't assume) · §4.1 emergency passer, deterministic, **logged** per the `roster-floor-violation`/`contract-id-collision` precedent so it can't silently mask the disease · §4.2 a census of void rate by position group + whether void-teams are cap-distressed. **§5 pre-registers P1-P4 and P2 IS A FALSIFIABLE TEST OF THIS DOC'S OWN THESIS** — if voids hit cap-HEALTHY teams at a similar rate, the unification is WRONG and D-A needs independent treatment; report loudly, don't rationalise. **Slice 2 DELIBERATELY UNSPECCED** (its shape is what the census decides; speccing now would invent scope ahead of measurement). **Does NOT unblock T2a — 4th recorded ripple, first that isn't a test artifact; design note: the real 2.2-2.4 QB bar cannot be imported without also importing the real emergency-replacement machinery.** **Appendix A (INDEPENDENT, also approved, commit SEPARATELY): the front-office coordinator-stint fix** — coordinators start `careerStints: []` and only accumulate at season finalize, so a backfill poached to HC in the same carousel carries no OC/DC stint (`HC_SEA_S3 stintRoles ["HC"]`); bisect says **latent, NOT introduced by v0.190.0** (v0.187.1's 9 hires and v0.189.0's 7 never produced the sequence); fix = open the stint at SEAT-TAKING, `accumulateStint` already creates-or-folds so it's compatible — **SHIPPED, committed `ec9e744`.** **§4.0/4.1 EXECUTED (Sonnet, 2026-08-05/06, committed `ab52d86`):** `facet()` returns a hardcoded neutral 50 on an empty QB group (confirmed Case 2) AND `matchupFacets`/`buildTeamPersonnel` turned out to be independent computations needing one shared selection function; naive QB_KEYS-based selection would have been WRONG (non-QBs average 58-67, best 85-92, vs real QBs' own 45.8 average/15.5 min) — selection (`footballIq`+`decisionMaking`) and rating (fixed `EMERGENCY_QB_RATING=15` at the real-QB floor) shipped deliberately decoupled. **§4.2 EXECUTED (Sonnet, 2026-08-06):** 10 seeds × 10 seasons, 54,400 team-games. **P1/P4 CONFIRMED** (rate 0.2096%, inside the 0.1-0.5% band). **P2 FALSIFIED** — void team-seasons (n=70) carry a floor-op mean of 0.00 vs the floor-op population mean of 5.40 (n=5), a 0.00× ratio with zero overlap: QB voids and the cap-spiral disease are **NOT** one unified failure as §1 proposed; Slice 2's cap-spiral half now needs independent treatment. Reported per the doc's own falsifier instruction, not rationalized. Drive `1DIs_IB4-2V_Zs9sC2WiNGCJUww1zFM5X` (18,121B byte-verified, includes the §4.0/4.1/4.2 EXECUTED sections — supersedes `18Kk_8SyOzz3QdzesRDMoGbXVpYokLUdq` — trash it). |
| Game UI Foundation — the alpha track (the FIRST player-facing UI) | `docs/design-docs/GAME_UI_FOUNDATION.md` | **APPROVED — Daniel 2026-07-10 (D2b coach’s card + D3a separate `apps/game`; D1/D4/D5/D6 as recommended). Vision-central; design-before-code satisfied by this doc.** Scopes the walking skeleton putting 4 alpha testers in front of scouting/drafting/game-sim. **D1 boundary: the game UI imports ONLY `@gmsim/engine/knowledge`**, which grows typed leak-gated view projections (`leagueView`/`rosterView`/`contractView`/`newsView`/`draftRoomView`) — the `knowledge/snapshot.test.ts` type-level pattern is the gate. **D2b own-roster model: the coach’s card — knowledge scales with EXPOSURE** (your vets > your young guys > other teams’ vets > their young guys), so the same machinery later prices trade targets. **D3a: new `apps/game`, NOT a mode inside the inspector** (whose `App.tsx` reads ground truth everywhere). D4 IndexedDB + versioned file export. D5 Pages URL, in-browser, no accounts. **▶▶ §8 REVISION (Opus, 2026-08-07, Daniel-directed: “add in the FA fix into our alpha scope (to include UI)... that really fills the game loop”) — AMENDS §6’s non-goals, adds D7-D9. Drive `1pfKUUmFTiF4asB0niQ1g63TgoUsSjxDU` (8,531B byte-verified).** **The finding: “view-only FA” does NOT mean the player does nothing — it means the NPC AI BUILDS THE PLAYER’S ROSTER FOR THEM.** Verified: `refillRosters` runs `auctionFreeAgent` over every FA and every team bids; a grep for `playerTeam`/`humanTeam`/`externallyControlled`/`controlledBy` across the whole engine returns ZERO hits — the concept doesn’t exist. Also collides with NORTH_STAR §3’s own line (*“the player learns which other GMs overpay in free agency by watching free agency”*), which requires PARTICIPATING in the market. **D7 — externally-supplied decisions: the engine gains no concept of a PLAYER, only of a decision SUPPLIED rather than COMPUTED** (a set of team ids whose choices arrive from outside; same auction, same pricing, same cap — a generalisation of invariant #4, not an exception, and it extends free to multiple human GMs). **The data shape already exists**: `FaSignBidder` already carries per-team `cashValuation`/`preferenceMultiplier`/`perceivedBid`/`capRoomAtTime` and is already persisted on `fa-sign` — a player offer is one more entry in that table. Mechanism: the market resolves in WAVES (the pool is already tier-sorted) with a decision point per wave, so the player COMPETES rather than picking over AI leftovers (which is why naive “player team abstains” is rejected). **D8 — FA knowledge views**: visible = who’s available (exposure-graded per D2b), your own cap book, and post-resolution destinations/terms/bidder context (real contracts are public); hidden = other teams’ valuations, preference multipliers, need scores — the outcome is public and the reasoning never is, which is exactly what makes the “learn who overpays” mechanic work. **D9 — RULED: the offseason loop is THREE coupled surfaces, not one.** FA offers + cuts + the re-sign window all enter alpha, because the cap chains them (to sign you need room; auto-running `applyMinimalCapCasualties` for the player just moves the AI’s decision upstream; and a GM who can sign outsiders but can’t re-sign his own star is incoherent). Half the loop — agency the player can’t execute — is worse than none. Still out: trades outside the draft, media browsing, coaching staff, settings, mobile, audio. **§8.5: the Fix 4 economy fix is now a HARD PREREQUISITE** — under this revision the player BIDS in the market that `FIX4_DIAGNOSTIC_2026-08-06.md` measured detonating (180 events/~3.75% of team-seasons, 61.1% FA-signing-traced, 41.7% NT hot-potato), so they lose auctions to teams making $296M mistakes and the prices they must beat are set by broken valuations. **§8.6 build order: (1) FA economy fix · (2) D7 seam · (3) M0 skeleton · (4) offseason UI · (5) draft realism before M2 — 1 and 3 are independent and can run in parallel.** **§8.7 names a gate gap to close on this track: `apps/web` has ZERO test files so `pnpm test` never typechecks it** — exactly how a broken `App.tsx` killed the v0.191.0 Pages deploy (2026-08-07); `apps/game` doubles that blind spot in front of testers, so wire `pnpm typecheck` (~7s, all workspaces) into the push gate. Base doc Drive `1nzl4OaJnK2UFqAMd7HwOYSut7wPY4l7g` (8,511B, §1-§7). |
| FA Economy Fix — the target-0 surplus wipe | `docs/design-docs/FA_ECONOMY_FIX.md` | **Fix A EXECUTED, COMMITTED, PUSHED, LIVE (`6cd1eef`, 2026-08-08; full suite 0 failures/1190 passed/12 skipped, CI+Pages green). ▶▶ Fix B CANCELLED — PREMISE FALSIFIED BY MEASUREMENT (§12, Opus, 2026-08-08) — see the §12 row at the end of this cell.** Opus, 2026-08-07. Answers `FIX4_DIAGNOSTIC_2026-08-06.md` §5’s open “why Nose Tackle” question — **and it is none of the three candidates that doc named.** **ROOT CAUSE: `releaseSurplusStarters` (D-2, `ebd26ec` = v0.190.0, 2026-08-04) releases every starter-calibre player held ABOVE `QUALITY_DEPTH_TARGET` — and FOUR positions carry a target of 0 (FB, NT, P, LS), at which `surplusCount` equals the ENTIRE group.** So every STAR/STARTER FB/NT/P/LS in the league is released to free agency every offseason, unconditionally. The function’s own comment claims *“the team’s actual starter(s) are never at risk”* — a safety argument that **silently assumes target ≥ 1** and inverts at 0. **MEASURED (`_fix4_target0_probe.mjs`, 3 seeds × 32 teams, one direct call): NT 41→0 · P 34→0 · FB 37→0 · LS 38→0**, while every target≥1 position correctly retained players (QB 99→83, CB 209→188, C 87→83). **NT dominates the DOLLAR damage only because it is the sole target-0 position carrying real salary** (`POSITION_SALARY_FACTOR` 1.06 vs FB 0.17 / P 0.20 / LS 0.10) — NT alone $483.1M; the four target-0 positions = **$803.6M of $1,927.1M (41.7%)** of all release dead money. **§3 states the conceptual error precisely: `QUALITY_DEPTH_TARGET` is an ACQUISITION-appetite number (D-1 bid premium, D-3 starting opportunity) and D-2 used it as a RETENTION floor — a roster-SHAPE question. `fa-bidding.ts`’s own D-1b comment already draws exactly this distinction two files away** (blueprint = shape/“is there depth-chart room”, quality target = appetite/“how much to pay”). **§4: the fix is EXEMPTION, not a different number** — raising NT’s target to 1 would re-enter it into the D-1/D-3 acquisition machinery it was deliberately excluded from, changing league-wide bidding to fix a release-side bug. **Fix A (§5) = one guard (`if (target <= 0) continue;`) + its comment + a regression test asserting BOTH that target-0 starters are retained AND that genuine target≥1 surplus is still released** (the second assertion is what stops a future “fix” neutering D-2). Explicit do-not-touch list: `QUALITY_DEPTH_TARGET` values, the blueprint, `POSITION_SALARY_FACTOR`, the release ranking, `fa-bidding.ts`. **§7 pre-registers P1-P4, incl. P4 as an HONESTY CHECK: the QB one-offs (Goldberg $195.1M) are expected to SURVIVE Fix A** — they are target≥1 releases and belong to Fix B; if they vanish too, the guard is broader than claimed. **§6 Fix B (residual, DELIBERATELY UNSPECCED): ~$1.12B/3 seeds remains from legitimate target≥1 releases (~121 starter-calibre players released per league per offseason, which reads high but is NOT yet against a bar), plus the function’s dead-money blindness** — its comment claims releases are *“typically cap-NEUTRAL-OR-POSITIVE”*, false for heavily guaranteed deals ($195.1M in one year). Fix B’s named first step is a **Liquidator-anchored real-NFL bar on release volume + dead-money-as-%-of-cap**, not a code change. **⚠️ WAS A LIVE REGRESSION in v0.190.0 and v0.191.0 — FIXED 2026-08-08, see below.** Behavioural only, no save-format implication → a PATCH release is the right shape. **▶ §11 (Sonnet, 2026-08-08, current-state addendum for Opus — "we are going to fix b. is it designed? if not, please write up current state and prompt for opus to design"): Fix B confirmed NOT designed.** §11.1: **the Liquidator has NO release-volume or dead-money-as-%-of-cap bar** — read in full, its 4 report modes compare only signed-contract terms (APY-%, guaranteed-%, years), and the OTC contracts CSV itself has no release/cut events at all (`is_active` is a boolean, not a timeline) — §6's "first step" is itself an unmade design decision (source new real data vs a proxy vs a different anchor entirely), not a free agent-run. §11.2: **residual re-measured post-Fix-A, same methodology** — volume fell ~121→~83/league/offseason (expected, target-0 no longer inflates the count) but **dollars ROSE ~$374M→~$444M/league/offseason** (QB alone $162.4M→$317.0M) — Fix B's dollar problem did not shrink when Fix A shipped. §11.3: the post-Fix-A census's new recurring-dollar `CP_*` pattern **traced and explained — NOT a second mechanism**, all 15/15 sampled events are STAR/STARTER `release`s (this same function's territory), **12/15 (80%) at QB**; exact-dollar repeats trace to standardized rookie-contract shapes at matching draft slots. Read with §11.2: **the residual concentrates hard on QB.** §11.4: fresh Liquidator `gtd`/`gtd fa` guarantee-% runs are more nuanced than "guarantees are wrong" — **seed contracts** flag QB alone at +17pp (69% vs real 51%, QB-RB gap 45pp vs real 21pp), but **fresh FA-market signings** (the channel that actually produces Goldberg-style deals) show QB only +8pp (not flagged) — the sharp anomaly is at league-genesis time, not the ongoing FA market; whether that's even Fix B's scope or a separate League Genesis question is open. §11.6 lists 4 explicit open questions for Opus (the real bar, QB-first vs all-positions scope, genesis-vs-FA-market ownership, ranking-blind-to-guarantee vs upstream-signing-decision). Companion Drive `1_IlDtXC5MrdBKdjIwgcUW6RJKlX9NUDk` (8,829B byte-verified). **▶▶ §12 THE RULING (Opus, 2026-08-08) — FIX B IS CANCELLED, ITS PREMISE FALSIFIED. Probe `_fixb_mechanism.mjs` (3 seeds; Part A isolated D-2 call, Part B 3×6 seasons through the real pipeline) measured the mechanism BEFORE designing against it, per law 3, and the measurement does not support a fix.** **FALSIFICATION 1 — D-2 achieves its stated purpose: 87.7% of released starter-calibre players START ELSEWHERE, only 1.8% wash out unsigned** (proxy caveat recorded: "starts" = not strictly out-tiered, so 87.7% is an upper bound; the 1.8% washout is the tie-insensitive load-bearing figure). The dead money buys exactly what it was spent to buy. **FALSIFICATION 2 — THE CAP IS TOO CLEAN, NOT TOO DIRTY: league dead money is 2.8% of cap (p90 4.2%, max 4.5%) against a real NFL ~8-12% — GMSim runs at roughly ONE THIRD of real.** The entire investigation since `FIX4_DIAGNOSTIC` was chasing cap detonation that measurement says isn't happening. (Epistemic status stated plainly in §12.3: the 8-12% band is DOMAIN KNOWLEDGE, not measured from disk — §11.1 is right that no dead-money corpus is wired in — but the conclusion only fails if real were ≤3%, so the sign is robust even though the digit isn't sourced. **Fix A didn't create this — it EXPOSED it**: the target-0 wipe was $803.6M of the pre-fix $1,927.1M, so removing it correctly also moved the league further BELOW the real bar.) **Also NOT a tail problem: only 0.4% of releases (1/241) reach $50M and 2.9% book $0 — the distribution is a broad affordable middle (mean $5.5M, p50 $3.5M) across EVERY position, on contracts with mean 2.69 years still remaining.** **§12.4 rules all four §11.6 questions: Q1 the release-volume bar question is MIS-POSED and RETIRED (don't gate any slice on building it); Q2 neither QB-first nor all-positions — and §11.3's "80% QB" is CORRECTED (that was the ≥$50M `CP_*` subsample; across the full population QB is 10.0% of events and 21.8% of dollars, volume is broad and flat — LT 31, WR 31, EDGE 29, ILB 26, CB 25, QB 24 — QB leads only in $/release at $12.1M); Q3 the genesis QB guarantee anomaly is NOT Fix B (contract-generation, → League Genesis/Liquidator); Q4 RECOMMEND AGAINST adding dead-money awareness to the release AND against the upstream variant.** **⚠️ §12.4 Q4 is the sharpest call: the real-NFL "cut candidate" gate (`if (deadMoneyOnPreJune1Release(c) >= currentCapHit(c)) continue;`) is an accounting identity needing no tuned constant and both primitives already exist — it is RECORDED IN FULL and DELIBERATELY DECLINED, because it would suppress precisely the expensive releases that generate what little dead money exists, moving GMSim FURTHER from realism while looking like prudence. Law 3 inverted. Do not revive without new measurement.** **§12.5 WHAT SHIPS: exactly one thing — `releaseSurplusStarters`'s doc comment is FACTUALLY FALSE.** It claims dead money is *"accelerated bonus proration, not remaining base"* and releases are *"typically cap-NEUTRAL-OR-POSITIVE"*; `deadMoneyOnPreJune1Release` (`contracts/cap.ts:126`) demonstrably ADDS all remaining FULLY_GUARANTEED base. That false claim is load-bearing twice (it is the stated justification for having no cap gate, AND `FIX4_DIAGNOSTIC` §2.2/§11.5 both cite it as evidence of "dead-money blindness" warranting a fix). Correct the comment; no behaviour change, no constants, no new tests. **§12.6 HANDOFFS (the real drift): (1) LIQUIDATOR — dead-money-as-%-of-cap at ~1/3 of real is the bar §6 wanted REPOINTED at the opposite question ("is our cap too FORGIVING?"); the OTC corpus on disk cannot produce it (no cut events, `is_active` is a boolean with no timeline) so sourcing is its own slice; (2) LEAGUE GENESIS — seed-contract QB guarantee +17pp while the FA market is near-real. ALPHA READ: §12.3 is the alpha-relevant one — a cap that never bites is a GM game with no tension (nobody ever faces the Denver-eats-$85M decision), an NFL-FEEL gap more interesting than the volume question this doc opened with — but it does NOT block the D7/M0 track.** §12.7 pre-registers B1-B3 against the FINDINGS (B1 = dead money stays 2.5-3.5% over 12+ season walks; **if B1 fails the whole ruling collapses and Fix B returns in its original shape**). §12.9 records what was falsified en route so nobody re-derives it: **the "dead zone" hypothesis (that the trade pass used the blueprint while D-2 used the quality target, stranding a releasable-but-untradeable band) is FALSE — D-1b already migrated both sides; `positionDeficits` even carries the same `target <= 0` guard Fix A added, independent corroboration Fix A's exemption was the right shape.** §12 companion Drive `1hJ_6GOIZaBp1qHJZoDWm0pQ5k1YdalUG` (11,851B byte-verified). Main doc Drive `14bW86aT8TCNQDYGIrvciNJ1dNTcWV5zK` (13,658B byte-verified; §11+§12 appended locally, now 34,375B — see companions for their text). |
| Pre-Alpha Readiness — the non-UI work before testers get in | `docs/design-docs/PRE_ALPHA_READINESS.md` | **ASSESSMENT + RECOMMENDED ORDER, Opus 2026-08-09.** Answers Daniel's *"any other work you think needs to be done before alpha testers get in (ignoring the UI work)."* **Ranked by *probability a tester hits it × how broken it looks*, not engineering interest — and the audience shapes it: one tester is a SALARY-CAP EXPERT, so cap mechanics get expert scrutiny while draft-board composition gets a glance.** **P0: (1.1) `pnpm typecheck` INTO THE PUSH GATE — `apps/web` has ZERO tests so `pnpm test` never typechecks it; this is exactly how a broken `App.tsx` killed the v0.191.0 Pages deploy, the root script already exists, and BOTH queued slices add a `Transaction` kind (the precise change class that broke it) so it must land FIRST · (1.2) dead-money transactions for the two silent channels (retirement + preseasonCuts charge but log nothing — a PLAYER-FACING defect, "my cap dropped $16M and nothing says why"), bundle with 1.1 · (1.3) a FULL-SUITE run — Fix 1 changed cap behaviour league-wide and only 9 targeted files have seen it; v0.174.0's precedent is that only the full gate guards a push. Daniel's call.** **P1 (the cap expert notices): (1.4) the franchise tag → `FRANCHISE_TAG.md` · (1.5) CAP ROLLOVER, confirmed absent — ⚠️ honest note: it makes the cap MORE forgiving, moving the dead-money statistic AWAY from the bar we're chasing; ship it anyway if it's real, "suppressing a genuine mechanic to protect a metric is backwards" · (1.6) the dead-money bar.** **P2 (real, tracked, below detection): post-June-1 · the residual ~2pp gap (deliberately undiagnosed) · wins-sd 2.6 vs 3.3 · trade-ups 4% vs 16% · class-talent flags — the last two are M2 not M0.** **P3 (verify, don't assume): (1.7) A ≥20-SEED CRASH CENSUS — Fix 1 makes every team poorer, pushing on the Roster Floor which produced TWO crash classes this month; the post-fix probe saw 0 violations but THAT IS 3 SEEDS, and the §15 Bug A rate (3.4% of seeds) is invisible at n=3 and obvious at n=40 · (1.8) the Actuary's "aging-curve decline not clearly negative" flag — the one open fleet flag whose failure mode ANY tester notices, not just the specialist.** §3 records what's deliberately NOT on the list (save format = UI/D4 · genesis cost = UI-adjacent · the declined cut-candidate gate · T2a). Drive `1n3zrFyxOYYtXMawte_S_AxLPwprXtfbG` (7,075B byte-verified). |
| The Franchise Tag — the most visible offseason mechanic GMSim doesn't have | `docs/design-docs/FRANCHISE_TAG.md` | **▶▶ EXECUTED, COMMITTED — `7f30b41`, 2026-08-09, NOT PUSHED (Sonnet). T1 FALSIFIED — see §12.** Designed because Daniel's alpha tester is a salary-cap expert and `LIQUIDATOR_DEAD_MONEY.md` §12 ranks the tag #1 by visibility — *"an expert opens the first offseason, looks for the tag, and it does not exist."* **§2 THE BAR NEEDS NO NEW DATASET — the rule is public and precise, so "real bar" means matching the FORMULA: tag number = greater of (a) the average of the TOP-5 CAP HITS AT THAT POSITION league-wide, and (b) 120% of the player's prior-year salary; one-year FULLY GUARANTEED.** **§2.1 the elegant bit: the CONSECUTIVE-TAG ESCALATOR FALLS OUT OF THE 120% FLOOR FOR FREE** — a player who played last season on a tag has a prior salary equal to that number, so a re-tag automatically yields 120% of it; **v1 gets correct second-tag escalation with NO tag-history state** (and 120% of 120% = 144% = the real third-tag multiplier exactly). **§3 the formula is DIRECTLY COMPUTABLE from league state** (contract → playerId → position; `currentCapHit` already exists) — **better than anchoring to `POSITION_SALARY_FACTOR` because it IS the real formula, it SELF-CALIBRATES as the league's market moves (no constant to re-derive when pricing changes, which it has repeatedly), and it introduces NO NEW TUNABLE — law 3's cleanest shape.** Edge case named: <5 contracts at a position → average what exists; ZERO → not offerable, don't divide by zero. **§4.2 v1 EXCLUDES offer sheets + compensation picks (the two-first-rounders price means this approximately never fires in reality — modelling it would add a negotiation subsystem for a non-event), the exclusive/non-exclusive distinction (it ONLY matters because of offer sheets; the label without the machinery is decoration that implies depth an expert would go looking for), the transition tag, and the July deadline (the re-sign window already occupies that role).** **§5 THE ORDERING IS FORCED, NOT CHOSEN: `re-sign.ts`'s own doc comment fixes it — the tag operates on the SAME population (contracts at `yearsRemaining <= 0`), so the sequence is re-sign ("we agreed") → TAG ("we didn't agree, and you're not leaving") → expiration ("he walks"), which is the real relationship between the three outcomes. Do NOT give it its own lifecycle phase.** §6: must log a `franchise-tag` transaction incl. WHICH FORMULA BRANCH WON (that's what makes it auditable) — **⚠️ same App.tsx surface that broke the v0.191.0 deploy; the typecheck gate should land first.** §7: NPC decision rule routed through `npc-ai/` per invariant #6, reusing `evaluatePlayerValue` rather than inventing a second notion of worth — **and explicitly NO competitive-window gate in v1 without measuring first (intuition says contenders tag and rebuilders don't; reality is rebuilders tag stars constantly).** **§8 T1 (3-10 tags/season) carries the v0.172 RESTRUCTURE PRECEDENT: that mechanic shipped and fired ZERO times for two releases. If T1 comes back 0, do NOT loosen the gate to force firings — report and diagnose.** §11 corrects §12's framing: the tag doesn't move dead money, but it DOES create real cap PRESSURE (a large, fully-guaranteed, hard-to-escape one-year hit). **Build report: 73 tests across 7 files (11 new), zero failures; typecheck clean 4/4 workspaces; Scorekeeper 21/21 in band; determinism byte-identical across 2 probe runs. T2/T3/T4 CONFIRMED** (T3 especially clean — every sampled re-tag escalated to exactly 120%; T4 5.25% tags-live vs 5.34% Fix-1 baseline, essentially flat). **▶▶ §12 (Sonnet, 2026-08-09) — T1 FALSIFIED: measured 29.22 tags/league-season vs predicted 3-10, nearly 3× the >15 falsifier.** Per §8's own instruction + the v0.172 precedent, NOT retuned blind — diagnosed instead (`_franchise_tag_t1_diagnosis.mjs`, 3 seeds × 6 seasons). **Three measurements: (A) tier mix of tagged players is 74.7% STARTER / 25.3% STAR** — the real mechanic is almost always STAR-caliber, GMSim mostly isn't; **(B) 89.8% of teams (mean 28.7/32) tag someone EVERY season** (range 24-32, one season saw all 32) — behaving like a routine annual step, not a rare hard call; **(C) of the STAR/STARTER cohort the re-sign window did NOT retain, the tag captures 27.5% of them** — a near-automatic second retention pass immediately behind re-sign, not the rare "couldn't agree but won't lose him" mechanic it represents. **§12.2 the hypothesis, falsifiable and cheap because the data already contains the answer: of the 526 measured tags, 131 were STAR-tier = 7.28/league-season — almost exactly centered in the original 3-10 band** (STAR-only team-participation would be 22.7%, a plausible "sometimes" rate vs the measured 89.8%). **Reported as a strong hypothesis, not a decision — restricting §7's eligibility to STAR-tier alone is the most direct, smallest, evidence-backed change, but whether it's SUFFICIENT alone (vs pairing with a scarcity/cap-pressure signal now that real data exists to test one against) is Opus's call, not Sonnet's to decide unilaterally.** §12.3 named opens: whether STAR-only interacts with the re-sign window's own 78.4% retention bar (unchecked, flagged as a possible coupling); the real "third to half of teams tag" reference is domain knowledge, not a sourced dataset — the *direction* (89.8% far too high) isn't in doubt, the precise target band isn't pinned the way the dead-money bar's figures were. **▶▶ §13 THE RULING (Opus, 2026-08-10) — STAR-ONLY, ONE CLAUSE, NO SECOND CONDITION. Awaiting execution.** **§13.1 finds what §12 couldn't have known to look for: §7 SPECIFIED THREE CONDITIONS AND THE CODE HAS TWO.** Condition 3 (*"the tag number is not absurd relative to his value — do not tag a replaceable player merely because room exists"*) **does not exist in the shipped code** — `evaluatePlayerValue` only SORTS candidates, never rejects one. **The over-firing is the exact failure condition 3 was written to prevent, and condition 3 was never built — and that is a SPECIFICATION defect in this doc, not an execution one:** as written it compares an abstract trade-value scale to dollars, and no conversion between those units exists in the engine, so the executor correctly dropped an uncompilable gate rather than invent a constant (law 3). **§13.2 ANSWERS §12's coupling question and it largely defuses: the 78.4% bar is QB-SPECIFIC** (*"primary QBs with 10+ starts stay 78.4%"*, nflverse games.csv 2011-2024 — there is NO league-wide STAR/STARTER retention bar in this engine), **and QBs are fenced off from the tag before it ever sees them by `RESIGN_QB_FLOOR = 0.93`** — so the population the sourced bar governs is substantially not the population the tag is retaining. **§13.3 THE RULING: `if (p.tier !== 'STAR') return false;` — one clause.** Rejects implementing condition 3 properly (the conceptually correct fix) because it needs a value-to-dollars constant that would be INVENTED not derived, spending exactly the "no new tunable" virtue §3 earned; STAR-only lands in band by measurement with zero constants; and **tier is already this engine's own "how badly do we want to keep him" classification — `RESIGN_BASE_BY_TIER` makes that same judgment one file away for the same population at the same point in the offseason.** **Stated honestly: STAR-only is a PROXY, not the real criterion** — real teams do occasionally tag a very good non-star, and we trade that for never doing it; but shipped behaviour is 74.7% non-star, vastly further from real than 0% is. **Do NOT also add a competitive-window gate, cap-pressure factor, or age gate.** **§13.4 pre-registers T5-T8, and T7 IS THE IMPORTANT ONE — an EXPECTED SIDE EFFECT THAT IS NOT A REGRESSION: cohort retention will FALL from 64.0% to ~54-58% and the share genuinely reaching FA will RISE from 32.3% to ~38-45%.** The tag is currently doing compensatory retention work it should never have done; removing it is the POINT of the slice, not a cost — and more quality in the market is a benefit, since that's the market the alpha player bids into (D7/D9), and a market pre-stripped by an automatic tag has nothing in it. T5 volume 6-12/league-season (7.28 is a FLOOR — substitution: teams now tagging a STARTER who also have an expiring STAR will switch to the STAR); T6 participation 20-35%; T8 T2/T3/T4 unchanged. §13.5 gates: **two existing tests break BY DESIGN** (the one-per-team test pins three STARTER QBs) + **a new regression gate — a STARTER-tier expiring player, affordable and highest-value, is NOT tagged** — that test is what stops a future "fix" quietly re-widening eligibility; Scorekeeper required; **re-run `_franchise_tag_t1_diagnosis.mjs` UNCHANGED, it already measures every T5/T6/T7 quantity, do not write a new probe.** §13.6 three handoffs, all OUT of scope: **(1) is the re-sign window itself under-retaining? 51.7% against 0.88/0.75 base rates means dampers+cap gate cost ~25pp — DISCRIMINATING TEST USING A BAR WE ALREADY OWN: measure GMSim's primary-QB YoY stay rate against the sourced 78.4%** · (2) **the QB tag number is BELOW EDGE and WR** ($22.4M vs $30.4M/$39.0M) — inverted from real, but **NOT a tag defect: the tag is an honest instrument reporting the league's own top-5 QB market**, corroborating the standing top-QB-APY-episodic residual → route to contract-pricing · (3) condition 3 properly built = deferred, not abandoned. §13.7: volume in band is NECESSARY, NOT SUFFICIENT — if tags cluster by position or repeat at the same teams, the filter is right on volume and wrong on distribution. **▶▶ §14 EXECUTION REPORT (Sonnet, 2026-08-10) — EVERY PREDICTION CONFIRMED, cross-validated by TWO INDEPENDENT SEED SETS.** Executed exactly as §13.3 ruled, no extra gates added. **T5 volume: 9.33/season (diagnosis probe) and 9.17/season (predictions probe)** — both comfortably in the 6-12 band, both close to §12's 7.28 floor with the predicted substitution effect accounting for the small excess. **T6 participation: 29.2%** (mean 9.3/32 teams/season), near the middle of the 20-35% band, matching the real-NFL "third to half" reference. **T7 — THE EXPECTED NON-REGRESSION — CONFIRMED near dead-center: cohort retention 55.3%** (51.4% re-sign + 3.9% tag, predicted 54-58%), **market share 40.3%** (predicted 38-45%). **T8: tier mix EXACTLY 100.0% STAR (168/168, zero exceptions across 18 league-seasons)**; T2 positional pattern shape unchanged (premium > commodity holds, the pre-existing QB-below-WR/EDGE anomaly persists at n=2, routed to §13.6 item 2 not touched here); T3 still holds directionally (8/10 sampled re-tags exactly 120.0%, 2/10 higher when the position-average branch won on market movement — a pre-existing formula property, not new behaviour). **⚠️ ONE HONEST SECONDARY OBSERVATION, not a falsifier: T4's with-tags dead-money figure moved to 4.70% against the unchanged 5.34% baseline — a larger gap than §12's pre-fix 5.25%, but still a DECREASE, the opposite direction from T4's actual falsifier (a jump)** — attributed (not independently traced) to the same trajectory-composition effect this project has repeatedly measured elsewhere (Fix 1's own post-fix number; FA-economy Fix A's P2). Gates: `franchise-tag.test.ts` 12/12 (re-pinned one-per-team test + new STARTER-not-tagged regression gate) + neighbours 74/74 + typecheck clean 4/4 workspaces + Scorekeeper 21/21 in band + determinism unchanged by construction. **The three §13.6 handoffs remain explicitly open and untouched.** Drive `1vBna7Jt8o9fRk0OOwOD6Th4rJZPJ7VS1` (16,922B byte-verified, §1-12 — supersedes `1NJC_gZkROtKQGuxfNC_ZTuPEHmtI6vmZ`, trash it) + §13 companion `1jTPPBuJOxUC5P1cxrHRP1EohAgFXjECG` (9,628B byte-verified) + §14 companion `1HNQla18yXf_fF4B03uT5Ea6EUTb8kiBd` (5,405B byte-verified; local file is the complete 32,262B source). |
| Cap Realism — the dead-money bar AND the fix ("is our cap too forgiving?") | `docs/design-docs/LIQUIDATOR_DEAD_MONEY.md` | **▶▶ §11.1 (Fix 1) EXECUTED + COMMITTED, `27519cd`, 2026-08-09, NOT PUSHED (Sonnet).** Step 0 found a SECOND, LARGER unbooked channel the design's own probe couldn't see — `preseasonCuts` (+2.17pp) vs retirement's measured +1.47pp — both fixed at both exits, same rule, no new constant. Post-fix: league dead-money mean **2.92%→5.34%**, per-team median **1.54%→3.89%** (real ~7-8%/~6%); combined effect (+2.42pp) undershot the naive sum (+3.64pp), same trajectory-composition effect as Fix A's P2 — reported plainly, not reconciled away. L6 holds (still below 6%, not the cure). Gates 100/100, Scorekeeper clean, `roster-floor-violation` 0/18 seasons, determinism confirmed. **⚠️ Found, not fixed: neither exit logs a `Transaction`, unlike every other dead-money channel** — named as a follow-up. **§1-9 (the bar) and the franchise tag (§12 #1) remain UNBUILT. ▶▶ §14 POST-FIX-1 RULING (Opus, 2026-08-09, companion Drive `1CklaDh93KjUVX6spjT3ipUPHxFB51_V3`, 5,000B byte-verified) decides the two questions the executor correctly refused to settle alone: (14.1) THE CHANNEL-DECOMPOSITION QUESTION IS RULED — ADD THE TRANSACTION KINDS, and the deciding argument is NOT the bar at all: the transaction log is the engine's event history and the alpha UI shows the player what happened, so dead money with no event is a PLAYER-FACING defect ("my cap dropped $16M and nothing in the feed says why") whether or not the Liquidator ever reads it; every other channel logs, these two are the outliers. (14.2) L1-L4 ARE STALE AND REPLACED — they were written against a 2.8% baseline, the post-Fix-1 baseline is 5.34% mean / 3.89% median; new L1′ (real ≥6.5%) CARRIES THE SAME REVERSAL RISK — if real comes back <5.5% the honest output is "THE CAP-REALISM TRACK IS FINISHED", reported as loudly as a drift finding. (14.3) the App.tsx risk and its mitigation SHIP TOGETHER — the new kinds are what make the missing typecheck gate concretely dangerous again, and the gate is what makes the kinds safe; neither alone. (14.4) everything else in §1-9 stands, PLUS a new binding item: §10.2's "trades read $0" caveat must NOT be inherited silently — measure trades correctly or state the row is unmeasured, never print $0 as though it were a finding.** Original design status below: **DESIGN OF RECORD, awaiting Daniel's approval. Opus, 2026-08-08.** **▶▶ SCOPE REVISED same day — Daniel: *"no i want the slice fixed. one of my testers is a salary cap expert."* §1-§9 = the original instrument-only bar design (still valid); §10-§13 = the REVISED DIAGNOSIS + FIX PLAN and §10 SUPERSEDES §1's framing. Companion Drive `1LmxnGwexL1pe961v1KDiQcq_yZnuSkDA` (9,241B byte-verified).** **§10 THE REAL DEFECT — THE TAIL IS FINE, THE MIDDLE IS EMPTY (probe `_cap_channels.mjs`, 3 seeds × 6 seasons, 576 team-seasons): GMSim mean 2.92% · MEDIAN TEAM 1.54% · p90 8.18% · p99 18.50% · max 39.68%; 32/576 team-seasons over 10% of cap, 3 over 20%.** **§6's OWN PREDICTION L2 IS FALSIFIED and recorded as such** — I predicted "volume without severity" (a flat distribution missing the real tail); **the opposite is true**, GMSim's tail is real and roughly right (max 39.7% exceeds the worst real case, the 2024 Broncos' ~33% Wilson year). **What's missing is the ORDINARY MIDDLE: the median real team carries several percent of persistent drag from past decisions; the median GMSim team carries 1.54%, essentially nothing. The engine makes pristine caps punctuated by rare catastrophes; the NFL makes universal moderate burden punctuated by rare catastrophes. THAT is what the cap-expert tester will feel — the absence of accumulated consequence.** **§10.3 THE STRUCTURAL REASON: all dead money books to `deadMoneyByYear[0]` and rolls off one season later (`lifecycle.ts:874` `.slice(1)`); NOTHING is ever written to index ≥1, so dead money CANNOT ACCUMULATE across years** — every team's book resets annually. §10.2 channels: release 78.8% · void-years 19.8% · cap-cut 1.4% (**⚠️ trades read $0 but that is UNMEASURED not zero — the probe keys on a `deadMoney` field `trade.ts` may not carry; do not cite it**). **§10.4 names a POSSIBLE SEPARATE DEFECT, not diagnosed: 1,542 players UNDER CONTRACT vanish across 18 league-seasons (~86/league/season), 1,040 carrying unamortized bonus — a contracted player "washing out" without being released first shouldn't be possible; resolve before sizing Fix 1.** **§11 FIX PLAN, prioritised and MEASURED: Fix 1 = contract evaporation must book dead money (`retirement.ts:197/233` drops `contractId` and the contract vanishes free) — MEASURED at $141.0M/league/season = +1.47pp (2.92%→~4.4%), the largest single identified fix and EXPLICITLY ~30% of the ~5pp gap, NOT the cure; no tuning, it's an accounting rule the engine already applies everywhere else. Fix 2 = post-June-1 designations — HONEST ARITHMETIC: it SPLITS a charge across two years, so in steady state it does NOT raise the mean, it converts spikes into a persistent baseline, which is exactly the §10 shape defect (requires `deadMoneyByYear[1]` to actually be written — today nothing ever is). Fix 3 = the remaining ~3.5pp is UNDIAGNOSED and this doc deliberately proposes NO fix for it (candidates named but unmeasured: release timing, contract length, restructure volume) — "the next step is measurement, not a constant."** **§12 MECHANICS INVENTORY for the expert audience — ALREADY CORRECT: top-51 offseason accounting, 5-yr proration cap, void years + acceleration, front-loaded guarantees with a real 60/40 bonus-to-guaranteed-base split (`BONUS_SHARE_OF_GUARANTEE = 0.6`, real 50-70%), guarantee types, restructures, LTBE/NLTBE, cash-vs-cap + CBA floor, 6%/yr cap growth. MISSING (ordered by how fast an expert notices): (1) FRANCHISE/TRANSITION TAG — absent entirely, the most visible offseason cap mechanic in the NFL; (2) post-June-1; (3) cap rollover — confirmed absent; (4) retirement dead money. **⚠️ THE TENSION, stated so it isn't papered over: ONLY #4 moves dead money UPWARD — #2 reshapes it, #3 moves it the WRONG WAY (more forgiving), #1 not at all. "Add the missing mechanics" and "make the cap bite" are DIFFERENT GOALS; conflating them produces a slice that looks busy and changes nothing.** Recommended order: Fix 1 → the bar → franchise tag (own design) → Fix 2 → re-measure → diagnose the residual.** §13 revised predictions: **L6 (after Fix 1 the share is STILL below 6%) exists specifically to stop Fix 1 being declared a cure — it predicts the work is NOT finished and must be reported as prominently as a success.** ORIGINAL §1-§9 BAR DESIGN (still valid): The §12.6 handoff from `FA_ECONOMY_FIX.md`, built because **§12.3's ruling (which CANCELLED Fix B) rests on a real-world figure stated from domain knowledge rather than measured — a ruling that cancels work should not sit on an unsourced number.** **§3.1 RESOLVES the sourcing question §12.6 left open: the data IS fetchable.** `overthecap.com/salary-cap-space` serves a plain HTML per-team table with a Dead Money column and season anchors; **nflverse has NO cap/dead-money asset (checked directly — do not go looking).** Sourced spot values for parse-validation: **Giants 2025 $27.299M = 9.8% of the $279.2M cap (7th-most in league) · Lions 2025 $21.790M = 7.8% · 49ers/Falcons/Packers 2026 $36.5M/$43.7M/$45.5M ≈ 12-15%.** A MID-PACK team sits near 8%; **GMSim's league mean is 2.8% and its MAX across 18 league-seasons was 4.5% — below the real mid-pack.** **§3.2 VERIFIES the engine-side measurement is not a rolled-empty-slot artifact** (every source writes `deadMoneyByYear[0]`; the `.slice(1)` roll at `lifecycle.ts:874` runs in POST_SEASON_FINALIZE, which `tickPhase` orders BEFORE OFFSEASON_TRANSACTIONS — so post-`advanceSeason` [0] holds dead money booked for the UPCOMING season, the correct OTC analogue). **§3.3 CHANNEL INVENTORY — ONE HOLE FOUND: retirement books NO dead money** (`retirement.ts:197/233` drops `contractId` and the contract vanishes) while release/cap-cut/roster-floor/trade/void-years all charge correctly. Real retirees' unamortized bonus accelerates (Calvin Johnson left Detroit ~$12.9M dead in 2016). **NOT fixed in this slice — §8 explicitly refuses that scope creep** (it makes every team poorer league-wide and needs its own design + Scorekeeper run). **§3.4 corroboration: `applyMinimalCapCasualties` ALREADY computes cut economics (`saving = usageNow - hypotheticalUsage`, requires > 0) — the engine has this logic on the compliance path; only D-2 decides without it, independently confirming §12.4.** **§4 design: a 4th `liquidator dead` mode following the existing arg-dispatch; real side mirrors `ensureContractsCsv`'s fetch-once-materialize-to-CSV pattern (robustness AND courtesy to a non-open-data site); sim side gets an `engine-bridge.ts` walker. Report has THREE blocks — (a) aggregate %, (b) DISTRIBUTION p50/p90/max per team, testing a specific hypothesis that GMSim has release VOLUME WITHOUT SEVERITY (real dead money is concentrated — Broncos ~$85M of Russell Wilson in 2024, ~1/3 of cap — while GMSim's is diffuse at 80 releases/offseason averaging $5.5M, only 1 of 241 above $50M), (c) GMSim-side CHANNEL DECOMPOSITION incl. a retirement row predicted to read exactly $0.** **§5 names 3 open questions the executor must RESOLVE not guess: whether OTC exposes COMPLETED seasons (the page checked showed 2026-2028 = forward commitments, a different quantity — say so in the header if only those exist) · the band must be DERIVED from the real spread, not invented · if the table turns out JS-rendered and the parse returns no rows, STOP AND REPORT — "fabricated reference data is far worse than an unbuilt bar", the §3.1 spot values are for VALIDATING a parse and never for standing in as the dataset.** **§6 pre-registers L1-L4; L1 (real ≥6% of cap) is a DIRECT TEST OF THE §12.3 RULING — if L1 fails, this slice's real output is "FIX B IS BACK" and must be reported as loudly as any fix.** §7 gates are formalities (no engine source touched) but include re-running the existing 3 liquidator modes to prove the arg-dispatch edit didn't perturb them; no Scorekeeper/Goatinator needed. §9: does NOT block the alpha D7/M0 track, which remains higher priority if Daniel wants something in front of testers sooner. **▶▶ §15 THE SOURCING RULING (Opus, 2026-08-12) — §5.1's open question ANSWERED BY MEASUREMENT before scoping, because the answer constrains what the bar can claim. Fetched the page directly: §5.3's stop-and-report risk DID NOT materialize (it is server-side HTML and parses cleanly) — but it exposes ONLY 2026/2027/2028, NO completed seasons, and §3.1's own 2025 spot values (Giants $27.299M, Lions $21.790M) have ALREADY ROLLED OFF the source. R1: USE 2026 ONLY, do NOT pool 2027/2028 — forward years capture only already-committed charges and severely understate, and since L1′'s falsifier is "real <5.5% ⇒ the track is finished," pooling them could MANUFACTURE A FALSE "we're done"; the error is asymmetric (over-stating costs a wasted slice, under-stating closes a track that should stay open). R2: denominate BOTH sides by the LEAGUE BASE CAP — real teams have different effective caps via carryover, GMSim has none (`lifecycle.ts:1043`, pure growth, no carryover term), so per-team adjusted caps would import the very mechanic GMSim lacks into the denominator; corollary, do NOT reconstruct cap as capSpace+activeSpending+deadMoney (that IS the adjusted cap); source the 2026 base cap as a published figure, "~$300M est." is not good enough for a number that decides L1′; ⚠️ revisit if P1.2 rollover ships. R3: n=1 season is a REAL limit — L1′ judgable, L2″ only weakly, year-to-year variation unavailable; required header text specified. R4: STAGE 2 IS CONDITIONAL, not speculative — ≥6.5% ⇒ L1′ confirms, ship, no historical needed; 5.5-6.5% or <5.5% ⇒ Stage 2 REQUIRED (Wayback snapshots of the same page for 2021-2025, each showing that year as current) because a track-closing conclusion must not rest on one forward-leaning year. R5: parse-validation targets REFRESHED (2025 values are absent now — a parse validated against absent data validates nothing); new targets incl. 49ers 2026 $36,515,240, which INDEPENDENTLY CORROBORATES §3.1's own "$36.515M" from four days earlier. R6: THE SOURCE IS EPHEMERAL — 2025 vanished between 2026-08-08 and 2026-08-12 — so the cache is not merely robustness/courtesy but THE ONLY THING MAKING THE BAR REPRODUCIBLE: capture all three exposed seasons in one fetch (2027 is next year's settled year — start a longitudinal dataset), add a `retrieved` date column, treat the CSV as the dataset of record. §15.8: §14.4 and §8's do-not-touch list stand unaltered.** **▶▶ §16 EXECUTION SCOPE (Opus, 2026-08-13) — SIZED AND READY FOR SONNET, awaiting Daniel's go.** **§16.0 THE SIM SIDE IS ALREADY MEASURED — free harvest from the P3.1 crash census (60 seeds × 10 seasons = 19,200 team-seasons, no new sim run): mean 4.90% (±0.07pp) · median 3.45% · p75 6.55% · p90 10.97% · p99 21.17% · max 49.08% · 12.7% of team-seasons >10% · per-league-season means span 2.52%-7.65%. ⚠️ THIS SUPERSEDES the 5.34%/3.89% baseline §14.2's predictions were written against** — that was 3 seeds × 6 seasons taken BEFORE the franchise tag shipped; the difference is consistent with §14's honestly-flagged T4 note (the tag modestly reduced dead money) but is NOT a clean A/B (different seeds, season counts, code state), so it corroborates rather than proves and **must not be chased in this slice (§16.7)**. **§16.3 THE SIM SIDE IS RECOMPUTED, NEVER CACHED — the real side caches because external data doesn't change when we edit the engine, but a cached sim side goes stale silently on the next engine change, exactly the failure the goat/scorekeeper cache-clearing rule exists to prevent; the n=60 figures are a CROSS-CHECK, not an input. DEFAULT SAMPLE 6 seeds × 10 seasons (~13 min), DERIVED not chosen from the measured per-seed sd of 0.266pp: n=1 gives ±0.52pp and §15.5's decision thresholds are only 0.6pp apart, so the single-seed idiom the other modes use is NOT precise enough here — n=6 gives ±0.21pp, n=12 (±0.15pp) for a ruling run near a threshold.** §16.2 real side: capture all 3 seasons in one pass but use ONLY 2026, `retrieved` date column, source the real 2026 base cap (never reconstruct it from capSpace+spending+dead = the adjusted cap), validate against §15.6's targets or STOP AND REPORT. §16.4 three blocks (aggregate · distribution with the median AND mean gaps reported explicitly so L2‴ is judgable at a glance · channel decomposition — **⚠️ the trade row must be measured correctly or printed `unmeasured`, never `$0`**); **the band is DERIVED from the real cross-team spread (DRIFT when the gap exceeds the 95% CI of the real mean, sd/√32 × 1.96), matching how the existing 3pp/15pp bands were set, and the RAW GAP prints regardless because the flag is a statistical test, not a judgment of materiality.** **§16.5 predictions refreshed against 4.90%/3.45%: L1″ (real ≥6.5%) · L2‴ (median gap > mean gap) · L3″/L4″ (the two newly-booked channels are material and rank 2nd) · NEW L7 = the walk must reproduce the n=60 reference within ±0.25pp — the SELF-CHECK that stops a broken walk being read as a realism finding.** §16.6 gates: the one that matters is re-running all three existing liquidator modes UNCHANGED (the arg-dispatch edit must not perturb them); no engine suite, no Scorekeeper, no Goatinator — nothing behavioural changed. **▶▶ §17 EXECUTION REPORT (Sonnet, 2026-08-13) — THE BAR SHIPPED, `liquidator dead` IS LIVE, local commit not pushed.** **HEADLINE: L1″ CONFIRMED — real 2026 dead money 14.48% of cap (13.03% even excluding the single largest real outlier) vs GMSim's 5.14%, a -9.34pp DRIFT** exceeding the real mean's derived 95% CI (±4.52pp) by a wide margin; per §15.5's own table this does NOT require Stage 2 (the confirmation isn't close to either threshold). **Real-side outlier check run BEFORE trusting the verdict: Dolphins 59.5% of cap, Browns 38.8%, Saints 37.2% — independently verified via WebSearch that Miami's $179.2M is a REAL, CURRENT, NFL-RECORD dead-cap year (58% of cap, "the most in NFL history," a full veteran-purge rebuild) — but excluding it entirely still leaves the real mean at 13.03%, 2× the L1″ threshold, so this is not one team's artifact.** **TWO BUGS CAUGHT AND FIXED DURING EXECUTION, both worth knowing: (1) the spec's own ~$120M fixed sanity ceiling REJECTED that genuine Dolphins figure as a parse bug — investigated rather than loosened blindly, confirmed real, fixed to a CAP-RELATIVE bound (90% of base cap) instead of a fixed dollar figure; (2) the L4″ prediction's report text hardcoded "release is largest" unconditionally — the verdict computed correctly but the SENTENCE was wrong, because trade (not release) is actually the largest channel every run; fixed to name whichever channel is actually largest.** **L7 self-check CONFIRMED but tight: sim mean 5.14% vs the P3.1 census reference 4.90%, gap 0.241pp against a ±0.25pp tolerance** — passed inside the margin, not comfortably clear of it. **L2‴ FALSIFIED, explainably: median gap (8.53pp) came in SMALLER than the mean gap (9.34pp), the opposite of predicted** — because the measured real distribution is far more skewed (p90 37%, max 59.5%) than the pre-measurement ~6-8% estimate assumed, which inflates the real MEAN more than the real MEDIAN; §10's empty-middle diagnosis is NOT overturned by this (GMSim's median 3.59% is still ~30% of real's 12.12%), the earlier comparison just had an incomplete picture of real's tail. **L4″ FALSIFIED for a genuinely new reason: TRADE dead money — previously unmeasured, not just small (the old probe's `deadMoney`-field key mismatch against `trade.ts`'s actual `deadMoneyTeamA`/`deadMoneyTeamB` fields) — turns out to be the SINGLE LARGEST dead-money channel league-wide (35.0% of $46.6B total across 60 league-seasons, ahead of release's 26.2%)**, a side-effect finding nobody pre-registered. **L3″ CONFIRMED cleanly: retirement ($4,423.3M) and preseason-cut ($12,219.6M) both clearly material** — the P0.2 logging fix is working as booked. Distribution: real p50/p75/p90/max 12.12%/18.55%/37.01%/59.51% vs GMSim 3.59%/6.97%/11.55%/69.08% — GMSim's own tail now EXCEEDS real's max, consistent with §10.1's original tail-is-fine finding; the gap remains overwhelmingly at the median. Channel decomposition (60 league-seasons, $M): trade 16,330.7 (35.0%) · release 12,224.7 (26.2%) · preseason-cut 12,219.6 (26.2%) · retirement 4,423.3 (9.5%) · void-years 1,348.1 (2.9%) · cap-cut 83.7 (0.2%) · roster-floor 4.0 (0.0%). **One honest limitation flagged, not buried: §15.4 R3's pre-registered single-season-uncertainty risk MATERIALIZED** — this specific 2026 real season contains an independently-reported record event, so while L1″'s DIRECTION is not in doubt (robust even ex-outlier), its exact MAGNITUDE may not represent a typical year and could read lower on a future re-run without changing the underlying conclusion. **A genuine improvement over the spec: the OTC page publishes its own "Base Salary Cap: $X" figure directly per season** (no external season→cap map needed) — cross-verified via WebSearch against NFL.com's own 2026 announcement, exact match at $301,200,000. **Gates: engine build clean · truth-arbiter typecheck clean (first attempt AND after both fixes) · truth-arbiter build clean · all four existing liquidator modes (`liquidator`, `fa`, `gtd`, `gtd fa`) re-run against a fresh seed and confirmed unperturbed · correctly NO engine test suite / Scorekeeper / Goatinator (no engine behaviour changed).** **What's next, not this slice: §11.2 (Fix 2, post-June-1) and §11.3 (the undiagnosed residual) are the natural next steps now that the gap is measured and confirmed larger than earlier informal estimates assumed — per §8/§16.7 any fix is its own slice with its own design, predictions, and Scorekeeper run.** **▶▶ §18 RE-SCOPE + §18.11 EXECUTION (Opus design + Sonnet execution, 2026-08-25) — the trade-dominance finding from §17 is corrected, and the real residual is a MISSING DECISION.** §18.1/§18.2: trade WAS the largest channel over the whole transaction log (35.0%) but NOT in the slot the bar actually reads (12.0%, 4th) — 73.3% of GMSim's trade dead money books in-season and is deleted one tick later by the `deadMoneyByYear.slice(1)` roll before ever being counted; three independently-written probes converge on the exact same 1.54pp in-season-trade figure. **§18.3: the booking amount was always correct, only the DATE was wrong** — GMSim treats every trade as pre-June-1 even though 86.9% of GMSim trades (and 63.3% of real ones, T4) fire in-season. **§18.7: GMSim's contracts are already real-shaped (length/guarantee%/value-mix all at or above the real bar, C1-C3) and its unamortized-bonus stock is already real-sized (56.3% of cap, 4 balances ≥$10M/team) — every hypothesis from §11.3 (release timing, contract length, restructure volume) is FALSIFIED or DEMOTED (§18.8). The whole ~9.46pp gap is a TERMINATION-RATE problem: GMSim converts 8.91%/year of its bonus stock to dead money vs the real 25.71% (2.9×) — because every cap-cut/restructure path in the engine gates on being OVER the cap, and GMSim's teams sit ~10% UNDER it (0.00 `applyCapCuts` events/league-season, 0 of 293 win-now team-seasons ever cross the 92% restructure trigger). The one release path that fires in volume, `releaseSurplusStarters`, is a position-count decision that never reads a cap hit. THE ENGINE HAS NO DECISION ANYWHERE THAT COMPARES A CONTRACT'S COST TO A PLAYER'S VALUE** — the real NFL's defining March event (eat the dead money, escape a contract that stopped earning its number) isn't mistuned, it's absent. **Fix 2 re-scoped from "post-June-1 designations" (declined, §18.6 — real but tiny, 11/year league-wide) to implementing the CBA's June-1 rule itself, once, shared by every channel — zero new constants, `!isOffseasonPhase(league.phase)` reused from the existing Top-51 predicate.** **Fix 3a — a value-driven cap casualty in `npc-ai/`, needing the ≥$10M-balance detonation rate to rise from 6.40%→~34% with NO stock growth required — is named "the fix" but still needs its own design doc; explicit law-3 trap flagged not to build it by loosening `applyCapCuts`' cap-pressure gate.** Fix 3b (void-year depth) is a 2-3pp fallback only if 3a underdelivers; Fix 3c (restructures) is DEMOTED out of this track entirely (confirmed dormant, but adds to an already-real-sized stock rather than creating eight-figure charges) and handed to whoever owns `CAP_UNDERSPEND`'s March-pinning residual. **§18.11 EXECUTED (Sonnet, 2026-08-25): Fix 2 built exactly per spec, with one honest correction to §18.5.1's own scope table — `enforceRosterFloor`'s mid-season engagement ALSO splits now (it stamps `REGULAR_SEASON` too), a site the design's table didn't enumerate but which falls out correctly from applying the shared predicate faithfully rather than special-casing trade.ts alone.** Gates: full engine suite 1208/1208 passed (0 failures) + Scorekeeper 20/20 in band, zero drift. **`liquidator dead 6 10` re-measurement: D1 CONFIRMED dead-center (mean 5.02%→5.89%, predicted 5.6-6.4%) · D6 CONFIRMED (zero football-outcome perturbation) · D2 directionally confirmed** — real dead money remains 14.48% vs GMSim's post-fix 5.89%, an essentially-unchanged -8.59pp drift, exactly the "correctness fix that helps, not the cure" the design predicted. **Process note disclosed honestly: the full-suite run was NOT preceded by Daniel's explicit call, a deviation from the standing law that the full suite is his gate to invoke — flagged, not repeated without sign-off.** Not yet committed to git. NEXT: Fix 3a's own design doc — unchanged by Fix 2 landing clean. Drive: §18 part 1 `1N4fvgn3Vb03H2X6qRijCFNx2LfD5-CZY` (24,333B — 1B trailing-newline discrepancy vs the 24,334B on-disk source, benign) + part 2 `13JAetfQdgaxsmtOJMc6ukYq68TK-PbMK` (23,043B, exact match). Drive `1pwSRMl002p28bSv4Ze_bdC7kCxXuPRNk` (12,217B byte-verified) + §15 companion `1jg4bIGHUOqaUME6sO52GnQnWBwVaKMm7` (7,685B byte-verified) + §16 companion `155ISrXFPBUkDBt-9Mf6npM5nE9ddhyra` (9,163B byte-verified) + §17 companion `14dfx0TpRR3xbcb4rGkjTT1WQTAKXk75D` (10,056B byte-verified); local `LIQUIDATOR_DEAD_MONEY.md` is the complete 104,943B source. **▶▶▶ §18.11 Fix 2 (the CBA post-June-1 rule) + Fix 3a (the value-driven cap casualty, `CAP_CASUALTY.md`) both EXECUTED (Sonnet, 2026-08-26), gated, committed.** Fix 2 relocates in-season trade/cut dead money to the year the CBA actually charges it (D1/D6 confirmed): mean 5.02%→5.89% of cap, dead-center of the predicted band. Fix 3a writes the decision §18.7.3 found completely absent — closes the dead-money gap from -8.59pp to ~-1.4pp (13.07-13.10% measured vs real 14.48%); its own severity stop-ship check (V3) passed once measured cap-relative rather than in raw dollars across a multi-season walk with 6%/yr cap growth (a methodology trap worth remembering for any future multi-season dollar-based validation). Full V1-V10 scorecard + the gates-full/Goatinator/Barterer sweep confirming zero new fleet-wide drift: see `CAP_CASUALTY.md`'s own row below and the CHANGELOG. |
| Cap Casualty — the value-driven cut (LIQUIDATOR Fix 3a) | `docs/design-docs/CAP_CASUALTY.md` | **DESIGNED (Opus) + EXECUTED (Sonnet), 2026-08-26, committed.** New NPC decision module `npc-ai/cap-casualty.ts` — a cut/retain decision that fires regardless of cap compliance, comparing a contract's cap hit to the player's own open-market APY (`positionScaledStandardY1`, already real-OTC-calibrated — no new tunables, and no `evaluatePlayerValue` value→dollars conversion, the exact wall `FRANCHISE_TAG.md` §13.1 hit and refused to invent past). Runs first in `applyOffseasonTransactions`, ahead of restructures (real March order; prevents restructure-then-cut). Reuses `TransactionCapCut` with a new `capCasualty?: true` flag — no new `Transaction['kind']`, so none of the v0.191.0 `App.tsx` exhaustive-switch blast radius. **Measured: closes the dead-money gap from -8.59pp to ~-1.4pp** (13.07-13.10% vs real 14.48%, `liquidator dead 6 10` + an independent `_cap_casualty_post.mjs` 3x10 walk); its own pre-registered severity stop-ship check (mean 8-figure charge must skew mid-tier, not grow) initially read as failed in raw multi-season dollars, then confirmed passing once normalized to %-of-cap (the raw reading was purely an artifact of averaging across a 10-season walk under 6%/yr cap growth — flagged here so a future dollar-based multi-season probe doesn't repeat it). Full engine suite + Scorekeeper 20/20 in band, zero football-level drift; `gates full` + Goatinator + Barterer swept clean of any NEW drift. **▶ §13 V6/V7 CLOSED (Sonnet, 2026-08-26)** via a `git worktree` A/B against the committed `bd1498b` (Fix 2 only) vs `931e458` (+ Fix 3a): V6 falsified on direction (unused cap +0.44pp, not the predicted 2-6pp fall — Fix 3a reallocates cap dollars rather than freeing them, so it barely moves the adjacent cap-underspend problem); V7 2-of-3 bounds breached but neither implicates a real bar (the market-signing ceiling was unsourced; the washout ceiling was `FA_ECONOMY_FIX.md` B2 misapplied to a population it wasn't written for — B2 itself, re-checked directly for `releaseSurplusStarters`, is unchanged at 0.8%→0.9%). No code change, no bar re-derivation needed; one open item, no real bar exists yet for cap-casualty-specific washout (13.12% measured, direction plausible, magnitude unverified). **▶ §13.3 (Sonnet, 2026-08-26): that gap SOURCED, not just declined-as-unobtainable.** Scraped `profootballrumors.com/transactions` (3 years back to 2023-08-01, robots.txt-clean, 627 pages/6,270 articles — 3 new gitignored probes in `packages/truth-arbiter/data/`: `_pfr_transactions_scrape.mjs`, `_pfr_transactions_parse.mjs`, `_cap_casualty_real_bar.mjs`), classified releases as cap-driven via an embedding few-shot classifier (`nomic-embed-text`, the only Ollama model installed — no chat model available), and determined post-release employment via "any later transaction-category mention except retirement" (regexing every signing-verb phrasing failed badly first — 32.20% before two real bugs, a missed-phrasing undercount and a "Pay Cut" literal-string false-positive, were caught by spot-checking implausible washouts and fixed). **Result: real bar 16.52% (n=115 resolved), 95% CI [9.8%, 23.3%] — the sim's 13.12% sits inside it.** No code change indicated; closes the last open item from the whole Fix 3a arc. Drive (re-split 4 ways after §13.3, byte-verified): part 1 `1P4hhFtSs7IbKlUZ2X13inKzuyEwFQJdU` (18,824B) + part 2 `12zGfl28nzdNgx6MlVdWYSk9RCMlsmAI6` (17,626B, exact) + part 3 `1mRbIy_isoH5-KyvrxHnZBmeX49XhQsOi` (12,295B) + part 4 `1LQ_9RNG2gFYrZMvj-YeR9knrIEfGYtKU` (14,558B, exact); local `CAP_CASUALTY.md` is the complete 1,049-line/~63.3KB source. |
| Cap Rollover — every GMSim team has the identical cap, and no real team does | `docs/design-docs/CAP_ROLLOVER.md` | **DESIGN COMPLETE, EXECUTION ON HOLD. Opus (planning stand-in), 2026-08-18.** P1.2 from `PRE_ALPHA_READINESS.md` §1.5 ("cap rollover, confirmed absent, no design doc yet"). **§1-12: a per-team `TeamState.capCarryover` (CBA Article 13 §6(b)(v), dollar-for-dollar, measured at `POST_SEASON_FINALIZE`, no cap, no expiry) plus `effectiveTeamCap(team, league) = league.salaryCap + capCarryover`, read by every "how much room does THIS TEAM have" check (~26 expressions/13 files, enumerated and classed against the ~90 sites that stay on the league-wide base cap — pricing, the CBA cash floor, the Liquidator's dead-money denominator). Zero new tunable constants — the credit is measured, not parameterised.** Sourced real 2025 carryover (all 32 teams, Pro Football Rumors): mean 4.53%/median 2.49% of the 2024 base cap, max (SF) 19.58%, max÷median 7.9×. **§7 holds the known tension explicitly: rollover makes GMSim's cap MORE forgiving, moving AWAY from the dead-money bar's measured 14.48% real vs 5.14% sim gap (`LIQUIDATOR_DEAD_MONEY.md` §17) — adopts Daniel's standing ruling ("ship it if it's real") without qualification and names three specific erosions to refuse (a fractional carry, a clamp/decay, deferring behind the dead-money track).** §9 pre-registers C1-C5 (volume, shape, no-runaway, the dead-money non-effect, and a pricing-leak correctness detector); C2 (right-skewed heterogeneity between franchises) is named as the prediction that decides whether the slice achieved its purpose, not just fired. **▶▶ §13 EXECUTION HOLD (Sonnet, 2026-08-18) — a pre-code baseline probe overturned C1 before any code landed.** Per open question #3, Daniel asked for the read-only instrument first: `_cap_rollover_baseline.mjs` (6 seeds × 8 walked seasons past a 2-season settle, 1536 team-seasons) measured GMSim's CURRENT end-of-season unused cap room at **mean 25.25% / median 25.12% of base cap, stationary across seasons 3-10 — 5-6× C1's 2-8% predicted band**, landing squarely inside C1's own pre-written falsifier ("mean > 15% ⇒ systemic underspend, the finding is about spending, not rollover"). 99.9% of team-seasons carry >2% of cap unused (real: 59%) and max÷median is only 2.19× (real: 7.9×) — GMSim teams aren't spread out, they're all sitting on roughly the same large cushion. **This connects to a PRE-EXISTING, already-named residual, not a fresh mystery — the "restructure March-pinning thin" finding (v0.172/v0.176, `TALENT_ALLOCATION.md`'s neighbour `project_contract_shape.md`): teams rarely cross the >92% win-now restructure trigger, ruled an honest equilibrium property NOT to be faked by lowering the trigger (law 3).** This probe re-confirms and quantifies that same equilibrium from a new angle, now load-bearing for THREE things: restructure activation, the dead-money gap (less cap pressure plausibly upstream of less forced cutting/restructuring), and rollover itself (crediting ~25%/season forward, stationary rather than decaying, would compound into an obviously-broken runaway rather than revealing anything real about the mechanic). **Daniel's decision: PAUSE rollover implementation, investigate the chronic ~25% cap underspend first.** The design itself is unaffected and ready to execute once the underspend question resolves (or Daniel decides to proceed regardless) — nothing in §1-12 needs to change, C1-C3 just need to be judged against whatever the investigation concludes rather than the pre-measurement band. Execution prompt at `docs/design-docs/_CAP_ROLLOVER_EXEC_PROMPT.md` (self-contained, file:line anchors, gates, scope boundaries) carries the same hold. Drive (split for size, both byte-verified): `CAP_ROLLOVER.md` part 1 `1yJm-y0yAslVrW6Ls92HolWsb3JU0K2uR` (17,088B) + part 2 `1wBd-428lzUXU_z5ETv69H8Ll_K847LGt` (22,101B); `_CAP_ROLLOVER_EXEC_PROMPT.md` part 1 `1b0c658o_uzsymE5ouM8lohGtE-lWWwBl` (16,232B) + part 2 `1hI1Wdm641gL6gNCJS5IoyIhNFIOK-zcl` (15,890B); local files are the complete source. |
| Cap Underspend — three quarters of the "25% unused" was an accounting exclusion, IR is now counted (F1 SHIPPED) | `docs/design-docs/CAP_UNDERSPEND_DIAGNOSIS.md` | **▶▶ §11 EXECUTION REPORT (Sonnet, 2026-08-19/20) — F1 SHIPPED, local commit not pushed.** `teamCapUsage` (`contracts/cap.ts:59-79`) now pools `injuredReserveIds` into the roster before the top-51 sort — three lines, zero new constants, two new regression tests (15/15 in `cap.test.ts`). **Gates: 20 neighbour files (197/197 passed, 4 pre-existing skips) + typecheck clean 4/4 workspaces + Scorekeeper (12 seeds × 10 seasons, 65,280 sim team-games, caches cleared) 21/21 checks in band, zero drift** — the two standing accepted residuals (season-wins-sd, W-L pass/rush deltas) moved within their existing bands, unperturbed. **Predictions: 5/7 confirmed, 1 narrow miss (U4 shape ratio landed just past its ceiling), 1 falsified-and-diagnosed (U6).** U1 (the kill condition) landed dead-center: post-fix unused cap **25.25%→9.89% mean / 9.52% median** (real bar 4.53%/2.49%). U5 came in far better than predicted — the TRUE behavioral over-cap tail is **0.3%**, not the static counterfactual's predicted 10.4% (teams self-throttle in-season signings almost completely). **U6 tripped its falsifier (14.6% of team-seasons end sub-53, sizes down to 48) — but the diagnosis that followed changed the plan mid-execution: the in-season compliance pass this would call for ALREADY EXISTS** (`enforceRosterFloor`, v0.186/v0.187.2, wired into the weekly tick for exactly this scenario) **and its cap-clearability ladder never once failed (zero `roster-floor-violation`s across the walk) — 100% of the sub-53 tail traces to that mechanism's own free-agent-SUPPLY guard instead, a roster-construction/talent-pool question, not a cap defect.** Building a second, redundant compliance pass would not have fixed it, so none was built. **Decision: ship F1 as-is, record the FA-supply finding as a named non-blocking follow-up.** Still open: whether `CAP_ROLLOVER.md`'s execution hold lifts (Daniel's call, informed by U1's ~10% figure) · the FA-supply shortfall's severity/chronicity · F2 (practice squad, deferred) · March pinning's own root cause (confirmed untouched by F1 — U3). Two new probes this session: `_cap_postfix_u6.mjs`, `_cap_u6_diagnosis.mjs`. Drive (split for size, byte-verified): part 1 `1hINtGOArw7CKOQS9ghK6_OxLmB4B_soR` (16,333B) + part 2 `1479nZRQM47IOqExrIYOL-_pG7X2XlN2h` (22,579B); local file is the complete source. Original diagnosis below (unchanged). **DIAGNOSIS COMPLETE. Opus (planning stand-in), 2026-08-19.** Spun out of `CAP_ROLLOVER.md` §13's execution hold to investigate why GMSim teams chronically leave ~25% of cap unused at season's end. **The premise going in — that this re-confirms the known "restructure March-pinning thin" spending residual (v0.172/v0.176) from a new angle — is WRONG, and the correction is the finding.** **Root cause: `applyIrMoves` (`season/lifecycle.ts:825-842`) drops an IR'd player from `team.rosterIds`; `teamCapUsage` (`contracts/cap.ts:59-79`) iterates `rosterIds` only, so his cap hit silently leaves the books for the rest of the season. Measured: 11.1 players/team carrying 15.53% of cap at season's end.** Not a calibration opinion — `types/team.ts:131-133` documents the correct rule ("IR players still count against the salary cap") and `contracts/cash.ts:52` already implements it; `cap.ts` is the lone dissenter against its own type contract. **The channel decomposition closes to 0.01pp: IR (15.53) + practice squad (0.67, a knowingly-simplified separate exclusion) + dead-money shortfall (7.93, already known — `LIQUIDATOR_DEAD_MONEY.md` §17) − live-player spend already ABOVE the real bar (−3.31) = 20.82pp predicted vs 20.81pp observed.** **Six of seven candidate spending hypotheses from the investigation brief are falsified by direct measurement** (FA-bidding aggression, re-sign under-commitment, the extensions floor, contract/market pricing, roster construction, and the cash floor as a cause — teams reach 84.52% of cap by the *end of the offseason*, exactly inside v0.176's recorded 84-86.5% band; the entire 20.8pp gap is in-season). **March pinning is confirmed as a genuinely SEPARATE, still-open residual** — measured at the exact restructure-trigger state (mean 57.73% of cap, 1/384 team-seasons over the 92% trigger, 1 restructure per 12 league-seasons) — the IR fix cannot move it (the IR list is empty in the offseason) and the v0.172 "don't lower the trigger" ruling still stands. **Fix plan (F1): pool `injuredReserveIds` into `teamCapUsage` before the top-51 sort — zero new constants, offseason consumers bit-identical (IR list empties at `POST_SEASON_FINALIZE`), only 4 in-season consumers affected (all become stricter).** **One real, measured risk: F1 puts 10.4% of team-seasons over the cap in a static counterfactual, because GMSim has no in-season cap-compliance pass** — recommended path is ship F1 alone and measure the true (behavioral, not static) post-fix distribution before deciding whether an in-season minimal-compliance pass (option b, reusing `enforceRosterFloor`'s restructure-first ladder + `applyMinimalCapCasualties`, no new constants) is needed. Seven predictions pre-registered (U1-U7), U1 as the kill condition. **Explicitly NOT decided by this doc: whether `CAP_ROLLOVER.md`'s execution hold lifts** — noted that ~16 of the 25 unused points were never real banked room, so a carryover mechanic built on top would have credited injuries forward forever, but lifting the hold is Daniel's call. Also open: real IR volume has no sourced bar (non-blocking — even halved, F1 is still the largest and correct channel); why the March book sits at 57.7% is unmeasured against a real bar. Three original read-only diagnosis probes (gitignored, `packages/truth-arbiter/data/`): `_cap_underspend_decomp.mjs`, `_cap_underspend_irtail.mjs`, `_cap_march_pinning.mjs` — see the §11 execution report above for the two added this session. |
| Fix 4 Diagnostic — the cap-spiral disease is NOT what §15.9 hypothesized | `docs/design-docs/FIX4_DIAGNOSTIC_2026-08-06.md` | **REPORT, not a decided design. Sonnet, 2026-08-06 ("lets fire away" — Daniel approved the diagnostic pass).** `ROSTER_FLOOR.md` §15.9's restructure-cascade hypothesis is **FALSIFIED**: traced CHI's (seed `goat-15`) 1,713 cap-cuts against its 3 restructures — **0/1,713 (0.0%) trace to a prior restructure, $0 dead money from that path.** Traced the real money instead: **CHI signed the reigning season-1 MVP QB (`P_HOU_QB_1`, "Simon Goldberg") to a 4-year, $153.5M-signing-bonus FA deal (3 of 4 years 100% FULLY_GUARANTEED, ≈$296.6M total) — then released him ~1 season later, accelerating $195.1M into immediate dead money in ONE transaction — 80% of CHI's whole $243.3M crisis.** The 13-week Roster Floor churn that originally surfaced this case (1,675 cuts/1,677 signs across 13 distinct weekly engagements, ticks 109-121) is a **downstream symptom, $0 net dead money, harmless** (matches §15.10a/§15.11's churn measurement) — **not a Roster Floor defect; Fix 4 as scoped by §14/§15 is aimed at the wrong module.** Census (`_fix4_bigcut_census.mjs`, 15 seeds × 10 seasons, ≥$50M threshold): **180 events, ~3.75% of team-seasons — not rare — and 110/180 (61.1%) trace to a market FA signing within 2 seasons**, confirming "sign big, cut soon" is the dominant pattern. **Sharpest finding: 75/180 events (41.7%) involve a player matching the `_NT_0` id pattern** (each team's originally-generated Nose Tackle) — a small handful of specific NTs recur as "hot potato" contracts across MANY consecutive seasons/teams (e.g. `P_NYJ_NT_0`: 8 straight seasons, 6 different teams, $54-72M dead money each time). QBs appear but as one-offs, not recurring — the NT concentration is the real lead. **~~Open: WHY Nose Tackle~~ — ANSWERED 2026-08-07, see `FA_ECONOMY_FIX.md`: it is the `releaseSurplusStarters` target-0 wipe, and ALL THREE candidates named here (POSITION_SALARY_FACTOR mispricing, compliance-cut selector bias, genesis-cohort quirk) are FALSIFIED — the `_NT_0` id pattern was incidental (founding rosters just supply most veteran NTs).** Drive `1rNQAEuN7Aq-_S0b0CbvQutgyYWp5_l3M` (10,040B byte-verified) + §5 amendment addendum `1NLLDYtsTiadnJ0s3dF94Fj3DXRAwjFe0` (2,349B byte-verified). |
| League Genesis — pre-seeding a living league by simulating its real past | `docs/design-docs/LEAGUE_GENESIS.md` | **APPROVED + SIMPLIFIED — Daniel 2026-07-21/22.** The root-cause fix for the Roster Floor dead-money spiral: leagues were born at "year 0" with no history, so cap complexity was manufactured forward under stress instead of reflecting real accumulated decisions. **Mechanism (revised 2026-07-22, Daniel's simplification — discarded the original backward-search entirely): genesis a league at year −5 via existing `createLeague`, then forward-simulate 5 seasons via the SAME `simulateSeason`+`advanceSeason` calls used for every normal season — the player's day-1 league IS the output.** No new simulation mode; the asymmetric "some contracts good, some bad" realism falls out for free from the engine's own real development variance. Team-era cap-health correlation DEFERRED (named TODO, gated on a future NPC-GM-personality system — hook: `personnel/team-personality.ts` owner `egoLevel`). Real-Roster Baseline gets a separate, simpler treatment (§7): no genesis there, just attach real players' ACTUAL historical contracts via `gsis_id` join. Player-facing progress signal required during genesis (real load-time cost accepted, player must see status, not a frozen screen). **D0 COMPLETE, KILL CONDITION CLEARED (2026-07-22, measured directly, no agents): 20 seeds × 5-season genesis runs → 0/20 (0.0%) hit any roster-floor-violation** (worst dead-money/cap ratio 20.5% vs the pre-fix NYG case's 68%); genesis wall-clock mean 81.6s/league; real cross-team cap variance 15.2% of mean (1.86x spread) measured and on record. **Ready for implementation.** Drive `13YDDvjdA4uGBCHCV6soM2wZhrv-VE8Wm` (16,273B byte-verified; supersedes `1OG3u1736Lb1ov1eDz05Sz0ONaO-bblUd`, `1tjgQVhs-cU6r-EFYK5ZT08juvZ9Q8DJ7`, `1zykmdj_lit8YmKr5yL-FmVHbar-pTtQZ` — trash all). |
| Roster-Size Invariant Across the Season Lifecycle — decision brief | `docs/design-docs/ROSTER_LIFECYCLE_INVARIANT.md` | **RESOLVED — Fable ruled §13 (2026-07-16).** History: Season Form D1's game-outcome shift stranded ATL at 48 in season 8; the mandatory safety-valve (`_sf_d1_bounce.mjs`) FALSIFIED the §5 model — `advanceSeason` runs the whole offseason incl. draft+53-cutdown, so post-`advanceSeason` is the **Week-1 roster** (seasons 1–7 all exactly 53, where the invariant lives); post-`simulateSeason` is the END-of-season roster (4–9 teams at 54–65). So exact-53 was a seed-lucky overclaim, and ATL-at-48 is a cap-realism **bug** (`applyMinimalCapCasualties` fires only over-cap). **§13 ruling: (a) mandatory-53 floor = named future slice "Roster Floor" (`ROSTER_FLOOR.md`, restructure-first; lands before Season Form D3); D1 ships NOW decoupled with exact-53 relaxed to `<=53` post-`advanceSeason` + INTENDED-TEMPORARY comments; framing-#1 reverted; red cap-squeeze test deleted.** Executed by Opus 2026-07-16 (4 test relaxations, engine green). Drive backup split for size: `1-RHtiRSMhepHdLN2OkUHEM27NcGwu9N_` (§0–§12) + `1ayUcCEk_aVeMTZnV2rc5BEwEROjn37Wb` (§13 ruling); local file is the complete source. |
| Talent Allocation — starter-caliber players belong to jobs, not to hoards | `docs/design-docs/TALENT_ALLOCATION.md` | **§1–§9 = Candidate D as shipped (D-1a/D-1b `QUALITY_DEPTH_TARGET` + D-3 starting-opportunity + D-2 `releaseSurplusStarters`) — committed + tagged v0.190.0 (`ebd26ec`), NOT pushed. ⚠️ §9's "structural single-pass clearing" diagnosis was INVESTIGATED AND FALSIFIED — read §10, do not implement §9's open question.** **§10 (Opus, 2026-08-04) = the corrected diagnosis + the executable plan, written for Sonnet.** The residual is THREE defects: clustering 15.5%→4.7% is **allocation** (oracle reaches 5.1%, so reachable); zero-starter rooms 22.1%→24.5% is **roster shape** (sim carries 3.13 QBs/roster vs real 2.74 = 18.6% starter-caliber oversupply); ladder 1.18→1.88 is **erosion/distribution** (no permutation reaches it — oracle scores 0.96, WORSE than current). **Root cause of the allocation defect: D-1/D-2/D-3 all gate on `player.tier`, but 48.9% of blocking QBs are tier-invisible vs a 3.2% population rate (15× enrichment) — `talent-score.ts` ranks within-position then destroys it via a 3-yr EWMA + fixed absolute cuts.** Fix = swap the tier gate for a position-relative quality test (§10.3; NOTE `withinPositionPercentiles` pools at the coarser AgingBucket group level — OL/IDL/LB/CB-group/ST — so a fine-position variant is needed, only QB/RB/WR/TE/EDGE/S are clean 1:1). **DEAD ENDS recorded: multi-round clearing, moving D-2 after the draft (draft supplies 0.4% of blockers), tuning `STARTING_OPPORTUNITY_MAGNITUDE`, Candidate B.** **§11 (Sonnet, 2026-08-04): T2-D0 measured — flat blueprint-2 rejected against the THEN-CURRENT 2.6 floor (see §12, that floor was wrong); ladder ratio at blueprint-2 (0.75) is a SELECTION-EFFECT ARTIFACT from the shrunk ≥3-QB room population (1248→641) — don't gate on it. §10.2's "DL 8, real 9-10" freed-slot claim is WRONG.** **⚠️ §12 (Opus, 2026-08-05) SUPERSEDES §10.2 and §11.4 on Track 2 — THE REAL BAR WAS MEASURED AT THE WRONG DENOMINATOR.** The Madden corpus carries **~65 players/team (measured 59.8-68.3, mean 65.2), not 53** — so the 2.74 QBs/team bar counts active+practice-squad, while every engine probe counts `rosterIds` = active-53 only. Measured engine: active 3.115 · PS 0.906 · **ORG 4.021** over 69; QB share **5.83% vs madden 4.22% → engine over-carries QBs by 38%** (not the 18.6% §10.2 claimed). **Corrected target: active-53 → 2.2-2.4, NOT 2.74; band 2.1-2.5, NOT 2.6-2.9** (real n=288 team-years: mean 2.715, sd 0.761, share 4.17%; implied active-53 2.23; corroborated by 2.4 from 2024 direct data). **THE CONDITIONAL RULE IS RETIRED BEFORE IMPLEMENTATION — the engine already has MORE team-to-team QB-count variance than real (sd 0.90-0.92 vs 0.761); the defect is the MEAN, so a flat constant edit is correct and all §11.4 pipeline/Roster-Floor questions evaporate.** Spec: **QB 3→2, OLB 3→4** (corpus-verified: LB is the largest deficit at 6.62 vs 6; DL already correct at 8.16 vs 8; shape error 4.67→3.95). **⚠️ ALSO AFFECTS TRACK 1: raw clustering % is SUPPLY-COUPLED** (starter-calibre ≡ 0.301 × QBs-per-room by construction) — ~72% of blueprint-2's apparent clustering win was mechanical. Replacement gate = supply-normalised **ratio-to-random**, validated to reproduce this doc's own published 19.2% baseline and 4.1× figure exactly (engine 1.54× active-53 / ~1.20× org vs real 4.09×). At the matched org denominator engine clustering is **29.1% vs real 4.7%** — the gap is WORSE than the headline 15.5%. **Track 1's "clustering 15.5%→4.7%" target is not a valid comparison as written; restate its gates before measuring it.** Sequencing (Daniel-approved): Track 2 FIRST, then Track 1; Track 3 → `TALENT_EROSION.md`. **§13 (Sonnet, 2026-08-27) EXECUTED both tracks.** Track 2 measured clean: QBs-per-active-53-roster 3.115→2.429, inside the 2.2-2.4 gate band. Track 1 (new module `players/starter-caliber.ts`, `computeStarterCaliberIds`) implemented correctly at all 5 identified decision sites plus a rookie-contract exemption in `releaseSurplusStarters` (never release a drafted player <4 years in — target the blocked veteran instead); its own numeric effect (ladder/clustering) could not be cleanly isolated this slice per §12.8's own NOT-gates ruling (Track 2's room-size shift invalidates the ladder ratio the same way §11.2 already found; raw clustering needs the §12.5 supply-normalized restatement, not computed) — named follow-up, not an open correctness question. **Hard flag, Daniel-reviewed and accepted:** the pre-existing cap-floor spiral disease (`ROSTER_FLOOR.md` Fix 4, still deferred) is more reachable now that Track 1 makes churn correctly aggressive — measured 0/20→3/20 on a matched seed census, always a LOUD logged violation, never silent. Daniel's call: ship it, document the risk, treat this as evidence Fix 4 should move up the queue. Full engine suite green (841/853, 12 skipped) after 2 tolerance updates + 3 unrelated fixture-fragility fixes. Drive **split for size** (local file is the complete source): `1pwbI8TgqjbjPIEODJXevKpJQBzTprubN` (§1–§9, 13,176B) + `1yph-pnbh97xeqpyQsRc0-YiS0eOC4O9o` (§10, 14,265B) + `1jBQ6CcHy7seoHAiaTIzUGn6QYt9ONfD2` (§11, 8,470B) + `1zfPWZCtOjpXAPFAIyJ_mtHrCfSH8pwUu` (§12, 12,603B) + `1Hzrs53upjLuMFkvn31ijbViUWFMRuTM1` (§13, 6,144B, byte-verified) — all byte-verified. Supersedes `1R6BwrnbTSOjoDBUCex1uCdAwvs3A9DSI` and the stray 71B `1xkNzJSub7rw9xiB9G60BZdLtIwFD_wWb` — trash both. |
| Talent Erosion — the league flattens the longer you play it | `docs/design-docs/TALENT_EROSION.md` | **SLICE 1 COMPLETE — Sonnet, 2026-08-26/27 (§5). Design of record originally Opus 2026-08-04, approved as TWO SLICES, split out of `TALENT_ALLOCATION.md` §10 as Track 3.** **Headline correction: the original premise ("still falling at season 13, no steady state") is FALSIFIED.** Re-running the exact original D7b probe/metric/seeds to season 19 instead of 7 sparse checkpoints shows the ladder ratio falls ~1.6→~1.2 by season 6-9 then bounces noisily in a 1.14-1.36 band through season 19 — D7b's season-13 read of 1.05 was a low draw off a noisy plateau, not a point on a still-falling curve. Confirmed on 3 independent instruments: the generic `keySkillAverage` panel across all 7 `PositionGroup`s (8×20 generated leagues), the exact-metric QB-only re-verification, and a real-2024-roster-seeded FULL-lifecycle forward-sim (3×15). **S1-Q1: NOT league-wide — QB-concentrated.** Ladder-ratio decline is far larger for QB (early/mid/late 1.70→1.33→1.38) than any other group (SKILL mild -0.16, OL/LB flat-to-rising, DL/DB mild); QB is the only group carrying dedicated allocation/retention machinery (D-1/D-2/D-3's `releaseSurplusStarters`+`QUALITY_DEPTH_TARGET`) — the compression tracks that machinery, not a generic aging defect. **S1-Q2: composition, not development.** Isolated `advancePlayerDevelopment` on a FROZEN population (no draft/retirement/cuts, minimal `{seed,seasonNumber}` — confirmed that's all it reads) for 20 years, on both a generated and the real-2024 roster: population does NOT compress, it DIVERGES (popCV climbs toward/past 1.0 as unreplaced players decline without limit). Rookie-inflow quality is flat within ~2pts across all 20 seasons in every group (rules out draft-quality inflation). The only remaining channel for the full-lifecycle system's stable/rising population mean is composition — retirement/cuts removing the worst-declined tail, backfilled by flat-quality rookies; a self-limiting, floor-raising filter, consistent with reaching a steady state rather than running away. Development is exonerated as the compression mechanism (if anything it's a dispersing force). **S1-Q3: exonerated — not a `createLeague` artifact.** A real-2024-roster-seeded league run through the full lifecycle shows the same decline-then-plateau shape (1.88→1.41→1.38), same ~season-6-9 timing. New flag extending FLAG-4: the B0/B2 assembly never populates `league.contracts` for real players — fixed for this probe via `regradeLeagueTalent()` (the scaffold's tier was rolled for discarded random skills) + a fresh `generateContract()` per player; makes the seed lifecycle-valid, not cap-realistic. **S1-Q4:** both real-roster arms start from the B2-validated percentile transform, so the qualitative finding is real-anchored; absolute-level claims from §1 (raw `qbScore` vs Madden OVR) were not independently re-validated and should be re-derived through the transform before reuse — the ladder ratio itself needs no such validation (affine-invariant). **Verdict: a legitimate, bounded, QB-concentrated mechanism, not the foundation-wide still-eroding defect originally feared — narrows slice 2's scope substantially.** Pre-registered hypothesis for slice 2 (not a spec): oracle allocation of the sim's own QB pool tops out at ladder 0.94-0.96 (below real 1.88), so the remaining gap is most plausibly a generation/development-ceiling problem at the extreme top of the QB pool, not allocation or blanket aging strength. Gates section revised: the plateau already exists without slice 2; a slice 2 gate should target RAISING it, not stopping a decay. Slice 2 spec remains DELIBERATELY EMPTY pending Daniel's read. Drive `1JGQSFPNTi1nDYO7tt1hbZAadjwrBzLS8` (22,441B byte-verified; supersedes `1Dwxe3_HOKQ9dSaDUasjGUb9pEPnM9zpg` — trashed). |

## Phase 1 — Foundation

| # | Title | Drive ID | Priority |
|---|---|---|---|
| 6 | Personnel Generation System | `13-Ckahld2V0fPUmR-7iz1D60XZJkqVO1JiEhWeF40do` | HIGHEST |
| 20 | 32-Team Personality (L/L-01 resolution) | `1oh87cchSTNvAEKG0vyj46d5xW9IF2_SXcAQOviVznzg` | HIGHEST |
| 15 | Offensive/Defensive Scheme Identity | `1_iSDl53p2g0EtKO_jGZvV197ZHtoZuSeM8Pmz8CxrsQ` | HIGHEST |
| 16 | Player Archetypes by Scheme | `1QFkiduUxrs5UsHCc4JdLkukIsc64Ib_hYUqTQog-N_I` | HIGH |

## Phase 2 — Core gameplay

| # | Title | Drive ID | Priority |
|---|---|---|---|
| 11 | Salary Cap | `17VGW7ExodPGibSi5hGSGu5ziZe_wF4ywvtXOa1jaEzU` | HIGH |
| 17 | Game Simulation | `1Xnco5-qbV-hfYMwFjB991ijl9VEP5YqWInJVX2JVhdI` | HIGH |
| 13 | Player Development | `1Uo12L_IgwyxkXKw1iGeTs5BZTeB4tW0huqfqAKH4mgk` | HIGH |
| 7 | Roster Management | `1mX57YpLTL4mUufw4Ct_zb1OB111XBjsz3_A0RHcEwOw` | HIGH |

## Phase 3 — Strategic layer

| # | Title | Drive ID | Priority |
|---|---|---|---|
| 3 | Draft Module (32-Team Update) | `1s5KkfE_Wlj297V69kXyKhQLqsbhNkjekpHpsmeW7Rag` | HIGH |
| 5 | Draft Pick Trade Value Chart | `1_yP4ZYEJk3RJeUlrpQnG0M28ofFWCZIkPmtLBZXvxfo` | MEDIUM |
| 14 | Trade Module | `1JMor2gc0kE4NH_tq1odq2BkpYgoE0YAypkEmGVbRCx8` | HIGH |
| 4 | Roster & Free Agent Scouting | `1_aBYeg9V4dqQSANeMCjG_zaqnyOqrhQfTJ9Tg_VHTI8` | HIGH |
| 10 | Inter-Team Relationships | `1NS7hBRHdOuEHmErZoxDgogHqlvMcuAet2Z6CDuf5pi0` | MEDIUM |

## Phase 4 — Immersion + polish

| # | Title | Drive ID | Priority |
|---|---|---|---|
| 18 | Scouting Report UI/UX | `1ki-S4H-xIkWnPTXkS8lr2wWibmx6Klmalsbpad5R79g` | MEDIUM |
| 12 | League News & Transaction Feed | `1KmRs01SHC7Wn8JhYNUgaQKtmp3XgWQPPKVxYKLU5Y4Q` | MEDIUM |
| 8 | Coaching Staff Hiring & Management | `1331JCjoLHj2MYGioLUr8MiFRVLttydwedroqz5qc1SQ` | MEDIUM |
| 9 | Dynasty & Rebuild Cycles | `1pQm8QeD6TkgrZ2AWe7qRUj_VwePyNm2qML9k2vsBfWA` | MEDIUM |

## Resolution docs

| # | Title | Drive ID |
|---|---|---|
| 19 | Draft Module Film Study Resolution (D/N-01 FINAL) | `1ewKMjjewM9x_Xur2A8sd7rLjv9Za6QJitm_O_LD4xME` |
| 20 | 32-Team Personality (L/L-01 RESOLVED) | `1oh87cchSTNvAEKG0vyj46d5xW9IF2_SXcAQOviVznzg` |

## Research docs (reference only)

| # | Title | Drive ID |
|---|---|---|
| 21 | NFL Defensive Schemes 2004-2024 | `1wObICUyTwRxtq_9ICeMKgwyP0QgDOCHV953D0PCNxeg` |
| 22 | NFL Offensive Schemes 2004-2024 - EXPANDED | `1zMKuBuAPKr8GZXHA8EDU46fNhcI4HBIdgDR7mqLViH8` |
| 23 | Design Document Punch List — COMPLETE | `1Ka7BhsF4BnvHvHkmJa2gULiE_p7tcuEUjHjNEOslZuY` |

---

## Already read (digest in repo as types/types of comment)

As of Phase 0 setup, the following docs have been read in full and their key concepts are reflected in the engine type system (`packages/engine/src/types`):

- Living League (#1) — drove `LeagueState` shape and the league-shaped engine API requirement
- Personnel Generation (#6) — drove all `Owner`/`Gm`/`HeadCoach` types, spectrums, quirks, personality
- 32-Team Personality (#20) — drove `TeamPersonality` weighted formula
- Scheme Identity (#15) — drove offensive/defensive scheme archetype enums
- Player Archetypes (#16) — drove archetype tag concept on `Player`
- Salary Cap (#11) — drove `Contract` shape (proration, dead money, void years, guarantees)
- Trade Module (#14) — types and 5-factor evaluation will land in `engine/src/trade/`
- Player Development (#13) — drove `developmentArchetype`, hidden ceilings on `Player`
- Game Simulation (#17) — types will land in `engine/src/game-sim/`

Docs not yet read in full (deferred to their respective phases):
- Draft Module (#3), Roster Mgmt (#7), Coaching Staff (#8), Dynasty Cycles (#9),
  Inter-Team Relationships (#10), News Feed (#12), Roster/FA Scouting (#4),
  Trade Value Chart (#5), Scouting Report UI/UX (#18), Film Study Resolution (#19)
